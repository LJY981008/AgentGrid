apiVersion: 1

providers:
  - name: agentgrid
    orgId: 1
    folder: Agent Grid
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
