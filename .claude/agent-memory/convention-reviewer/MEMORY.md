# convention-reviewer 메모리

- 규칙 원본 3종: .claude/rules/{backend-conventions,logging-rules,frontend-conventions}.md — 전부 "초안" 상태 (코드 0줄 시점 작성). 첫 구현 후 실측 예시로 갱신될 예정 — 갱신되면 검사 기준도 따라감
- 역할 경계: 로직 버그 = superpowers:code-reviewer / 컨벤션 위반 = 나. 중복 지적 지양
- 규칙 문서에 없는 지적 금지 — 새 규칙이 필요해 보이면 "rules 추가 제안"으로 분리 보고
