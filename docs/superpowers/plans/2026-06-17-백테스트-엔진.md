# 백테스트 엔진 (M2) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** stockpick 룰을 시간축으로 굴려 검증하는 백테스트 엔진(`src/stockpick/backtest/`)을 자체 구현 — 무료 1년치로 골격 검증, 결제후 구조변경 0으로 확장.

**Architecture:** `rules`(랭킹) 위층에서 rolling as_of 리밸런싱 루프를 돌린다. 저장소 읽기는 `ports.py` Protocol로 추상화(DI), 포트폴리오 구성은 `strategy.py` 플러그인. 돈=Decimal 끝까지, 통계(Sharpe·CAGR)=float 1곳 격리. 룩어헤드(랭킹 t·진입 t+1)·생존편향(UniversePort constituents)·과적합(IS/OOS·purge·decay) 가드 내장.

**Tech Stack:** Python 3.12, Decimal, DuckDB(기존 `_scan` 재사용), pytest, ruff/mypy(strict). 외부 백테스트 라이브러리 없음(ADR-004).

**검증 게이트(모든 커밋 후):** `docker compose exec -T app sh -c 'ruff check src tests && ruff format --check src tests && mypy && pytest -q'`

**⚠️ work-history 규약:** Task 1에서 승인 플랜을 `docs/work-history/2026-06-17-M2-백테스트엔진.md`로 백업 + INDEX 행 추가(첫 src 커밋 전). drift 훅이 src 변경 커밋에 엔트리 없으면 차단. 완료 시 After 채워 마지막 커밋에 포함.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `src/stockpick/backtest/__init__.py` | 패키지 docstring(스텁 → 구현 전환) |
| `src/stockpick/backtest/config.py` | `BacktestConfig`(frozen) + `fingerprint()` canonical 해시 |
| `src/stockpick/backtest/calendar.py` | `rebalance_dates(trading_days, freq)` 거래일 기준 리밸일 |
| `src/stockpick/backtest/costs.py` | `apply_cost_fraction(turnover, cost_bps)` 순수 bps 마찰 |
| `src/stockpick/backtest/strategy.py` | `Strategy` Protocol + `EqualWeightTopN`·`ScoreWeightTopN` |
| `src/stockpick/backtest/ports.py` | `PriceSeriesPort`·`UniversePort`·`IdentityResolver` Protocol |
| `src/stockpick/backtest/adapters.py` | Parquet/DuckDB 프로덕션 구현체(`_scan` 위임) |
| `src/stockpick/backtest/fakes.py` | 테스트/데모용 합성 포트(합성 폐지 포함) |
| `src/stockpick/backtest/metrics.py` | `BacktestResult`·`GuardReport` + `compute_metrics` |
| `src/stockpick/backtest/engine.py` | `run(config, ports, strategy)` 리밸 루프·forward-return·폐지청산 |
| `src/stockpick/backtest/benchmark.py` | `equal_weight_universe` 등가중 벤치 곡선 |
| `src/stockpick/backtest/validation.py` | `walk_forward`(IS/OOS·purge) + `decay_ratio` |
| `src/stockpick/backtest/demo.py` · `__main__.py` | 무료 9종목 골격 시연 |
| `tests/test_backtest_*.py` | 모듈별 pytest(합성 — 라이브 0) |
| `docs/decisions/ADR-004-백테스트-프레임워크-자체구현.md` | §9-3 결정 기록 |

의존방향: `types ← data·rules ← backtest`. `backtest`는 `rules`·`data`·`types`만. `engine`은 ports를 **인자 주입**(전역 금지). `adapters`·`fakes`만 `_scan` import.

---

## Task 1: 스캐폴딩 — ADR-004 + work-history 백업 + 패키지 docstring

**Files:**
- Create: `docs/decisions/ADR-004-백테스트-프레임워크-자체구현.md`
- Create: `docs/work-history/2026-06-17-M2-백테스트엔진.md`
- Modify: `docs/work-history/INDEX.md` (행 추가), `docs/HOME.md` (ADR-004 MOC 링크)
- Modify: `src/stockpick/backtest/__init__.py`

- [ ] **Step 1: ADR-004 작성** — 결정: 자체구현(Decimal·DuckDB). 맥락: §9-3 미해결. 대안 vectorbt(float·블랙박스)·backtrader(과설계) 기각. 근거: 기존 rules/ Decimal·DuckDB·룩어헤드 가드 재사용, 정밀도 BLOCKING, 1인 유지보수·과적합 투명성. `docs/templates/adr-template.md` 형식.

- [ ] **Step 2: work-history 엔트리** — 승인 플랜 전문(`/home/code/.claude/plans/pro-3-5-twinkling-adleman.md`) 백업 + 의도/Before(backtest 빈 패키지·validated=false 영구) 실측. After/SHA는 완료 시. `docs/templates/work-history-template.md` 형식. INDEX.md 행 + HOME.md ADR-004 링크 추가.

- [ ] **Step 3: `__init__.py` docstring 전환**

```python
"""backtest 모듈 — 룰 검증(생존편향·룩어헤드·과적합 가드). M2.

rules(랭킹) 위층: rolling as_of 리밸런싱으로 룰을 시간축으로 굴려 자산곡선·지표를 산출한다.
저장소 읽기는 ports(Protocol·DI)로 추상화 — 무료 1년치 골격과 결제후 다년 전체유니버스가
같은 코드로 동작(데이터량 무관). 돈=Decimal, 통계(Sharpe·CAGR)=float 경계 격리.

⚠️ 산출 지표는 데이터 신뢰성(S6) 게이트 통과 전까지 알파 아님(BacktestResult.data_caveats 명시).
"""
```

- [ ] **Step 4: Commit**

```bash
git add docs/decisions/ADR-004* docs/work-history docs/HOME.md src/stockpick/backtest/__init__.py
git commit -m "docs: ADR-004 백테스트 프레임워크 자체구현 + work-history 착수"
```

---

## Task 2: BacktestConfig + fingerprint

**Files:**
- Create: `src/stockpick/backtest/config.py`
- Test: `tests/test_backtest_config.py`

- [ ] **Step 1: 실패 테스트**

```python
from decimal import Decimal
from datetime import date
from stockpick.backtest.config import BacktestConfig


def _cfg(**kw: object) -> BacktestConfig:
    base = dict(
        strategy_name="equal_weight_top_n", top_n=5, lookback_days=126,
        skip_recent_days=21, rebalance_freq="monthly", cost_bps=Decimal("10"),
        delisting_recovery_rate=Decimal("0"), group_by_exchange=False,
        start=date(2024, 1, 1), end=date(2024, 12, 31),
    )
    base.update(kw)
    return BacktestConfig(**base)  # type: ignore[arg-type]


def test_fingerprint_deterministic_same_meaning() -> None:
    # Decimal("10") 와 Decimal("10.0") 는 같은 의미 → 같은 해시
    assert _cfg(cost_bps=Decimal("10")).fingerprint() == _cfg(cost_bps=Decimal("10.0")).fingerprint()


def test_fingerprint_changes_on_param_change() -> None:
    assert _cfg(top_n=5).fingerprint() != _cfg(top_n=10).fingerprint()


def test_fingerprint_is_hex_sha256() -> None:
    fp = _cfg().fingerprint()
    assert len(fp) == 64 and all(c in "0123456789abcdef" for c in fp)
```

- [ ] **Step 2: 실패 확인**

Run: `docker compose exec -T app pytest tests/test_backtest_config.py -q`
Expected: FAIL (ModuleNotFoundError: backtest.config)

- [ ] **Step 3: 구현**

```python
"""백테스트 설정 + 멱등 fingerprint. 전 필드 Decimal/int/date/str/bool — float 금지(재현성)."""

from __future__ import annotations

import hashlib
import json
from dataclasses import asdict, dataclass
from datetime import date
from decimal import Decimal


@dataclass(frozen=True, slots=True)
class BacktestConfig:
    """단일 백테스트 실행의 모든 입력(같은 config = 같은 결과). float 필드 금지(0.1 비재현)."""

    strategy_name: str
    top_n: int
    lookback_days: int
    skip_recent_days: int
    rebalance_freq: str  # "monthly" | "quarterly"
    cost_bps: Decimal  # 진입·청산 회전분 bps(수수료+슬리피지)
    delisting_recovery_rate: Decimal  # [0,1] 폐지 시 entry 대비 회수 비율(0=휴지, 1=마지막가)
    group_by_exchange: bool
    start: date
    end: date
    trading_days_per_year: int = 252  # 연환산 상수(명시)

    def fingerprint(self) -> str:
        """canonical 직렬화 → sha256. Decimal normalize·sort_keys 로 의미동일=해시동일."""
        return hashlib.sha256(
            json.dumps(_canonical(asdict(self)), sort_keys=True, separators=(",", ":")).encode()
        ).hexdigest()


def _canonical(obj: object) -> object:
    """Decimal→normalize 문자열, date→ISO, dict 재귀. float 유입은 명시 실패(재현성 BLOCKING)."""
    if isinstance(obj, float):
        msg = f"BacktestConfig 에 float 금지(재현성): {obj!r}"
        raise TypeError(msg)
    if isinstance(obj, Decimal):
        return f"D:{obj.normalize():f}"
    if isinstance(obj, date):
        return obj.isoformat()
    if isinstance(obj, dict):
        return {k: _canonical(v) for k, v in obj.items()}
    return obj
```

- [ ] **Step 4: 통과 확인** — Run: `docker compose exec -T app pytest tests/test_backtest_config.py -q` → PASS

- [ ] **Step 5: Commit**

```bash
git add src/stockpick/backtest/config.py tests/test_backtest_config.py docs/work-history
git commit -m "feat: 백테스트 BacktestConfig + 멱등 fingerprint"
```

---

## Task 3: calendar — 리밸런싱 날짜

**Files:** Create `src/stockpick/backtest/calendar.py` · Test `tests/test_backtest_calendar.py`

- [ ] **Step 1: 실패 테스트**

```python
from datetime import date, timedelta
from stockpick.backtest.calendar import rebalance_dates


def _trading_days(start: date, n: int) -> list[date]:
    # 주말 제외 근사(테스트용 — 실제론 데이터의 거래일 집합)
    days, d = [], start
    while len(days) < n:
        if d.weekday() < 5:
            days.append(d)
        d += timedelta(days=1)
    return days


def test_monthly_picks_first_trading_day_of_each_month() -> None:
    td = _trading_days(date(2024, 1, 1), 200)  # ~9.5개월
    reb = rebalance_dates(td, freq="monthly")
    # 각 (연,월) 첫 거래일만
    months = [(d.year, d.month) for d in reb]
    assert months == sorted(set(months))  # 월별 1개·정렬
    assert reb[0] == td[0]


def test_quarterly_subset_of_monthly() -> None:
    td = _trading_days(date(2024, 1, 1), 260)
    q = rebalance_dates(td, freq="quarterly")
    assert all(d.month in (1, 4, 7, 10) for d in q)


def test_unknown_freq_raises() -> None:
    import pytest
    with pytest.raises(ValueError, match="rebalance_freq"):
        rebalance_dates(_trading_days(date(2024, 1, 1), 10), freq="weekly")
```

- [ ] **Step 2: 실패 확인** — Run: `docker compose exec -T app pytest tests/test_backtest_calendar.py -q` → FAIL

- [ ] **Step 3: 구현**

```python
"""거래일 집합 → 리밸런싱 날짜(각 기간 첫 거래일). 합성 금지 — 데이터의 실제 거래일만 입력."""

from __future__ import annotations

from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from datetime import date

_QUARTER_MONTHS = frozenset({1, 4, 7, 10})


def rebalance_dates(trading_days: list[date], *, freq: str) -> list[date]:
    """정렬된 거래일에서 각 (연·월) [분기면 분기시작월] 첫 거래일을 리밸일로. 빈 입력=빈 리스트."""
    if freq not in ("monthly", "quarterly"):
        msg = f"rebalance_freq 는 monthly|quarterly (받음={freq})"
        raise ValueError(msg)
    ordered = sorted(trading_days)
    out: list[date] = []
    seen: set[tuple[int, int]] = set()
    for d in ordered:
        if freq == "quarterly" and d.month not in _QUARTER_MONTHS:
            continue
        key = (d.year, d.month)
        if key not in seen:
            seen.add(key)
            out.append(d)
    return out
```

- [ ] **Step 4: 통과 확인** → PASS
- [ ] **Step 5: Commit** — `git commit -m "feat: 백테스트 calendar 리밸런싱 날짜"`

---

## Task 4: costs — bps 마찰 순수함수

**Files:** Create `src/stockpick/backtest/costs.py` · Test `tests/test_backtest_costs.py`

- [ ] **Step 1: 실패 테스트**

```python
from decimal import Decimal
from stockpick.backtest.costs import apply_cost_fraction


def test_cost_fraction_basic() -> None:
    # turnover 1.0(전량 교체) × 10bps = 0.001
    assert apply_cost_fraction(Decimal("1.0"), Decimal("10")) == Decimal("0.0010")


def test_zero_turnover_zero_cost() -> None:
    assert apply_cost_fraction(Decimal("0"), Decimal("10")) == Decimal("0")


def test_negative_inputs_raise() -> None:
    import pytest
    with pytest.raises(ValueError, match="음수"):
        apply_cost_fraction(Decimal("-0.1"), Decimal("10"))
```

- [ ] **Step 2: 실패 확인** → FAIL

- [ ] **Step 3: 구현**

```python
"""거래비용 순수함수 — 회전율(turnover) × bps → 비용 분율(Decimal). 마찰만(폐지청산은 engine)."""

from __future__ import annotations

from decimal import Decimal

_BPS = Decimal("10000")


def apply_cost_fraction(turnover: Decimal, cost_bps: Decimal) -> Decimal:
    """turnover(편출+편입 절대비중 합, 단방향 0~2)·cost_bps → 비용 분율. equity *= (1-분율)."""
    if turnover < 0 or cost_bps < 0:
        msg = f"turnover·cost_bps 음수 불가(turnover={turnover}, bps={cost_bps})"
        raise ValueError(msg)
    return (turnover * cost_bps / _BPS).quantize(Decimal("0.0001"))
```

- [ ] **Step 4: 통과 확인** → PASS
- [ ] **Step 5: Commit** — `git commit -m "feat: 백테스트 costs bps 마찰함수"`

---

## Task 5: strategy — 포트폴리오 구성 플러그인

**Files:** Create `src/stockpick/backtest/strategy.py` · Test `tests/test_backtest_strategy.py`

설계 메모: `Strategy.weights(ranked, as_of)` 는 `list[TopEntry]` → `dict[str, Decimal]`(cik→비중, 합=1). TopEntry.cik 가 비어있으면(EODHD 미제공) ticker 를 키로 폴백(엔진이 IdentityResolver 로 cik 채운 TopEntry 를 넘기는 게 정석이나, 키 결정은 `_key(entry)` 헬퍼로 일원화).

- [ ] **Step 1: 실패 테스트**

```python
from datetime import date
from decimal import Decimal
from stockpick.types import Exchange, TopEntry
from stockpick.backtest.strategy import EqualWeightTopN, ScoreWeightTopN


def _entry(cik: str, rank: int, score: float) -> TopEntry:
    return TopEntry(cik=cik, ticker=cik, exchange=Exchange.NASDAQ, rank=rank,
                    score=score, rule_version="v0", factors={"momentum": score})


def test_equal_weight_sums_to_one_and_uniform() -> None:
    ranked = [_entry("A", 1, 0.5), _entry("B", 2, 0.3), _entry("C", 3, 0.1)]
    w = EqualWeightTopN().weights(ranked, as_of=date(2024, 6, 1))
    assert w == {"A": Decimal("1") / 3, "B": Decimal("1") / 3, "C": Decimal("1") / 3}
    assert sum(w.values()) == Decimal("1")


def test_score_weight_proportional_positive_only() -> None:
    ranked = [_entry("A", 1, 0.6), _entry("B", 2, 0.2)]
    w = ScoreWeightTopN().weights(ranked, as_of=date(2024, 6, 1))
    assert w["A"] == Decimal("0.6") / Decimal("0.8")
    assert w["B"] == Decimal("0.2") / Decimal("0.8")


def test_empty_ranked_empty_weights() -> None:
    assert EqualWeightTopN().weights([], as_of=date(2024, 6, 1)) == {}
```

- [ ] **Step 2: 실패 확인** → FAIL

- [ ] **Step 3: 구현**

```python
"""포트폴리오 구성 전략 — 플러그인(Strategy Protocol). 랭킹 결과 → cik별 비중(합=1, Decimal)."""

from __future__ import annotations

from decimal import Decimal
from typing import TYPE_CHECKING, Protocol, runtime_checkable

if TYPE_CHECKING:
    from datetime import date

    from ..types import TopEntry


def _key(entry: TopEntry) -> str:
    """비중 키 = cik(앵커). 미제공(빈 문자열)이면 ticker 폴백(데이터 한계 — caveat 대상)."""
    return entry.cik or entry.ticker


@runtime_checkable
class Strategy(Protocol):
    """랭킹된 TopEntry → {key: weight Decimal}(합=1). 순수 — 데이터·시간 의존 없음."""

    @property
    def name(self) -> str: ...

    def weights(self, ranked: list[TopEntry], *, as_of: date) -> dict[str, Decimal]: ...


class EqualWeightTopN:
    """동일가중 — 각 종목 1/N(§5 분산투자). 가장 단순·과적합 방어."""

    name = "equal_weight_top_n"

    def weights(self, ranked: list[TopEntry], *, as_of: date) -> dict[str, Decimal]:
        if not ranked:
            return {}
        w = Decimal(1) / Decimal(len(ranked))
        return {_key(e): w for e in ranked}


class ScoreWeightTopN:
    """점수가중 — 양수 점수 비례. 점수합<=0 이면 동일가중 폴백(음수 비중 금지)."""

    name = "score_weight_top_n"

    def weights(self, ranked: list[TopEntry], *, as_of: date) -> dict[str, Decimal]:
        if not ranked:
            return {}
        scores = {_key(e): Decimal(str(e.score)) for e in ranked}
        total = sum((s for s in scores.values() if s > 0), Decimal(0))
        if total <= 0:
            w = Decimal(1) / Decimal(len(ranked))
            return dict.fromkeys(scores, w)
        return {k: (s / total if s > 0 else Decimal(0)) for k, s in scores.items()}
```

- [ ] **Step 4: 통과 확인** → PASS
- [ ] **Step 5: Commit** — `git commit -m "feat: 백테스트 strategy 포트폴리오 플러그인"`

---

## Task 6: ports — 저장소 읽기 추상화 (Protocol)

**Files:** Create `src/stockpick/backtest/ports.py` · Test `tests/test_backtest_ports.py`

- [ ] **Step 1: 실패 테스트(Protocol 구조 적합성)**

```python
from stockpick.backtest.ports import PriceSeriesPort, UniversePort, IdentityResolver
from stockpick.backtest.fakes import FakePriceSeriesPort, FakeUniversePort, StubIdentityResolver


def test_fakes_satisfy_protocols() -> None:
    assert isinstance(FakePriceSeriesPort({}), PriceSeriesPort)
    assert isinstance(FakeUniversePort({}, {}), UniversePort)
    assert isinstance(StubIdentityResolver({}), IdentityResolver)
```

(`fakes.py` 는 Task 7에서 구현 — 이 테스트는 Task 7 후 통과. 지금은 Protocol 정의만.)

- [ ] **Step 2: 구현**

```python
"""백테스트 데이터 읽기 포트(Protocol·DI). backtest 는 라이브 API 가 아니라 저장소를 읽는다.

룩어헤드 BLOCKING: PriceSeriesPort.load(as_of) 는 랭킹용(<=as_of 만). full_series() 는 수익
실현용 전구간(엔진이 [t+1,t'] 만 잘라 씀). UniversePort.constituents(as_of) 는 가격파일 존재가
아니라 listed/delisted 기준 — survivorship 정답.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Protocol, runtime_checkable

if TYPE_CHECKING:
    from datetime import date

    from ..rules._scan import PricePoint


@runtime_checkable
class PriceSeriesPort(Protocol):
    def load(self, *, as_of: date) -> dict[str, list[PricePoint]]:
        """ticker별 수정주가 시계열(trade_date <= as_of). 룩어헤드 1차 가드."""
        ...

    def full_series(self) -> dict[str, list[PricePoint]]:
        """전구간 시계열(수익 실현용 — 엔진이 진입일~청산일 구간만 슬라이스)."""
        ...

    def trading_days(self) -> list[date]:
        """데이터에 존재하는 정렬된 거래일 합집합(calendar 입력)."""
        ...


@runtime_checkable
class UniversePort(Protocol):
    def constituents(self, *, as_of: date) -> set[str]:
        """as_of 시점 거래가능 ticker 집합(listed<=as_of and (delisted None or as_of<delisted))."""
        ...

    def delisting_event(self, ticker: str) -> date | None:
        """ticker 폐지일(없으면 None). 엔진이 보유구간 내 폐지 청산 판정에 사용."""
        ...


@runtime_checkable
class IdentityResolver(Protocol):
    def cik_for(self, ticker: str, *, on: date) -> str:
        """시점 on 에서 ticker 의 cik(안정 식별자). 미해소면 빈 문자열(caveat 대상)."""
        ...
```

- [ ] **Step 3: Commit** — `git commit -m "feat: 백테스트 ports 읽기 추상화 Protocol"`

---

## Task 7: fakes + adapters — 합성 포트(폐지 주입) + Parquet 구현체

**Files:** Create `src/stockpick/backtest/fakes.py`, `src/stockpick/backtest/adapters.py` · Test `tests/test_backtest_ports.py`(Task 6 테스트 통과 + 추가)

- [ ] **Step 1: 추가 실패 테스트(constituents·폐지)**

```python
from datetime import date
from decimal import Decimal
from stockpick.rules._scan import PricePoint
from stockpick.backtest.fakes import FakePriceSeriesPort, FakeUniversePort, StubIdentityResolver


def test_universe_constituents_excludes_future_and_delisted() -> None:
    uni = FakeUniversePort(
        listed={"A": date(2023, 1, 1), "B": date(2024, 6, 1)},        # B 는 2024-06 상장
        delisted={"A": date(2024, 3, 1)},                              # A 는 2024-03 폐지
    )
    # 2024-02-01: A 거래가능(상장·미폐지), B 미상장
    assert uni.constituents(as_of=date(2024, 2, 1)) == {"A"}
    # 2024-07-01: A 폐지·B 상장
    assert uni.constituents(as_of=date(2024, 7, 1)) == {"B"}
    assert uni.delisting_event("A") == date(2024, 3, 1)


def test_price_port_load_filters_as_of() -> None:
    series = {"A": [PricePoint(date(2024, 1, 2), Decimal("10")),
                    PricePoint(date(2024, 2, 1), Decimal("11"))]}
    port = FakePriceSeriesPort(series)
    loaded = port.load(as_of=date(2024, 1, 15))
    assert [p.trade_date for p in loaded["A"]] == [date(2024, 1, 2)]
```

- [ ] **Step 2: 실패 확인** → FAIL

- [ ] **Step 3: `fakes.py` 구현**

```python
"""테스트·데모용 합성 포트. 핵심 = FakeUniversePort 의 합성 폐지 주입(생존편향 가드 발동 증명).

무료 데이터엔 실제 폐지가 0건이라 가드가 한 번도 발동 안 됨 = 죽은 가드. 합성 폐지로 청산
경로·생존편향 sensitivity 를 TDD 로 봉인한다.
"""

from __future__ import annotations

from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from datetime import date

    from ..rules._scan import PricePoint


class FakePriceSeriesPort:
    def __init__(self, series: dict[str, list[PricePoint]]) -> None:
        self._series = series

    def load(self, *, as_of: date) -> dict[str, list[PricePoint]]:
        return {t: [p for p in pts if p.trade_date <= as_of] for t, pts in self._series.items()}

    def full_series(self) -> dict[str, list[PricePoint]]:
        return self._series

    def trading_days(self) -> list[date]:
        return sorted({p.trade_date for pts in self._series.values() for p in pts})


class FakeUniversePort:
    def __init__(self, listed: dict[str, date], delisted: dict[str, date | None]) -> None:
        self._listed = listed
        self._delisted = delisted

    def constituents(self, *, as_of: date) -> set[str]:
        out: set[str] = set()
        for ticker, listed_at in self._listed.items():
            delisted_at = self._delisted.get(ticker)
            if listed_at <= as_of and (delisted_at is None or as_of < delisted_at):
                out.add(ticker)
        return out

    def delisting_event(self, ticker: str) -> date | None:
        return self._delisted.get(ticker)


class StubIdentityResolver:
    """골격용 — 주입된 ticker→cik 맵. 미해소면 빈 문자열(caveat). 실전=ticker_history(후속)."""

    def __init__(self, mapping: dict[str, str]) -> None:
        self._mapping = mapping

    def cik_for(self, ticker: str, *, on: date) -> str:  # noqa: ARG002 (on=시변, 골격은 무시)
        return self._mapping.get(ticker, "")
```

- [ ] **Step 4: `adapters.py` 구현(프로덕션 — `_scan` 위임)**

```python
"""프로덕션 포트 구현체 — Parquet/DuckDB(_scan 위임). 데모·실전이 주입한다.

UniversePort: 무료 골격엔 종목마스터(listed/delisted)가 저장소에 없다(EODHD 미제공). 가격파일
존재로 유니버스를 도출하면 survivorship 결함(미래상장 포함·폐지 자동제외)이므로, 프로덕션
UniversePort 는 종목마스터 적재(TASK-E/S5) 후 구현한다 — 그 전엔 데모가 fakes 를 쓰고 한계를
data_caveats 에 명시한다.
"""

from __future__ import annotations

from typing import TYPE_CHECKING

from ..rules import _scan

if TYPE_CHECKING:
    from datetime import date
    from pathlib import Path

    from ..rules._scan import PricePoint


class ParquetPriceSeriesPort:
    """daily_bar Parquet → 수정주가 시계열(_scan 위임). full_series 는 as_of=None 전구간."""

    def __init__(self, base_dir: Path) -> None:
        self._base_dir = base_dir
        self._full: dict[str, list[PricePoint]] | None = None

    def load(self, *, as_of: date) -> dict[str, list[PricePoint]]:
        return _scan.load_adjusted_series(self._base_dir, as_of=as_of)

    def full_series(self) -> dict[str, list[PricePoint]]:
        if self._full is None:
            self._full = _scan.load_adjusted_series(self._base_dir, as_of=None)
        return self._full

    def trading_days(self) -> list[date]:
        return sorted({p.trade_date for pts in self.full_series().values() for p in pts})
```

- [ ] **Step 5: 통과 확인** — Run: `docker compose exec -T app pytest tests/test_backtest_ports.py -q` → PASS
- [ ] **Step 6: Commit** — `git commit -m "feat: 백테스트 fakes(합성폐지) + Parquet adapters"`

---

## Task 8: metrics — BacktestResult·GuardReport·지표

**Files:** Create `src/stockpick/backtest/metrics.py` · Test `tests/test_backtest_metrics.py`

설계 메모: equity_curve(list[(date, Decimal)]) + period_returns(list[Decimal]) → 지표. 돈/수익=Decimal, Sharpe·Sortino·CAGR·MDD=float(무리수·통계). MDD 는 Decimal 곡선에서 float 변환.

- [ ] **Step 1: 실패 테스트**

```python
from datetime import date
from decimal import Decimal
import math
from stockpick.backtest.metrics import compute_metrics


def test_known_equity_curve_metrics() -> None:
    curve = [(date(2024, 1, 1), Decimal("1.0")), (date(2024, 7, 1), Decimal("1.2")),
             (date(2024, 12, 31), Decimal("1.1"))]
    period_returns = [Decimal("0.2"), Decimal("-0.0833333")]
    res = compute_metrics(curve, period_returns, periods_per_year=2,
                          turnover_total=Decimal("2"), cost_total=Decimal("0.002"),
                          n_rebalances=2, n_delisted=0, benchmark_returns={},
                          caveats=("1년치",), config_fingerprint="abc")
    assert res.total_return == Decimal("0.1")  # 1.1/1.0 - 1
    # MDD: 1.2→1.1 = -0.0833...
    assert math.isclose(res.max_drawdown, -0.0833333, rel_tol=1e-3)
    assert res.n_delisted_liquidations == 0
    assert res.data_caveats == ("1년치",)


def test_empty_curve_safe() -> None:
    res = compute_metrics([], [], periods_per_year=12, turnover_total=Decimal("0"),
                          cost_total=Decimal("0"), n_rebalances=0, n_delisted=0,
                          benchmark_returns={}, caveats=(), config_fingerprint="x")
    assert res.total_return == Decimal("0")
    assert res.sharpe == 0.0
```

- [ ] **Step 2: 실패 확인** → FAIL

- [ ] **Step 3: 구현**

```python
"""백테스트 결과 계약 + 지표. 돈/수익=Decimal, 통계(Sharpe·Sortino·CAGR·MDD)=float 1곳 격리."""

from __future__ import annotations

import math
import statistics
from dataclasses import dataclass, field
from decimal import Decimal
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from datetime import date


@dataclass(frozen=True, slots=True)
class BacktestResult:
    config_fingerprint: str
    equity_curve: list[tuple[date, Decimal]]
    total_return: Decimal
    cagr: float
    sharpe: float
    sortino: float
    max_drawdown: float
    turnover: Decimal
    total_cost: Decimal
    n_rebalances: int
    n_delisted_liquidations: int  # 폐지 청산 건수(생존편향 가드 발동 증거)
    benchmark_returns: dict[str, float]
    data_caveats: tuple[str, ...]  # 미검증 한계(cik미해소·합성폐지·1년치 등)


@dataclass(frozen=True, slots=True)
class GuardReport:
    is_sharpe: float
    oos_sharpe: float
    decay_ratio: float | None  # IS_sharpe<=ε 면 None(분모 폭발 방지)
    is_failed: bool  # IS_sharpe<=0 → 룰 기각
    decay_warning: bool  # decay_ratio < 0.5
    purge_gap_days: int
    sensitivity: dict[str, float] = field(default_factory=dict)  # 후속(인터페이스만)
    notes: tuple[str, ...] = ()


def compute_metrics(
    equity_curve: list[tuple[date, Decimal]],
    period_returns: list[Decimal],
    *,
    periods_per_year: int,
    turnover_total: Decimal,
    cost_total: Decimal,
    n_rebalances: int,
    n_delisted: int,
    benchmark_returns: dict[str, float],
    caveats: tuple[str, ...],
    config_fingerprint: str,
) -> BacktestResult:
    """자산곡선·기간수익 → 지표. 빈 곡선이면 0 안전값(조용한 추측 금지)."""
    if not equity_curve:
        return BacktestResult(config_fingerprint, [], Decimal("0"), 0.0, 0.0, 0.0, 0.0,
                              turnover_total, cost_total, n_rebalances, n_delisted,
                              benchmark_returns, caveats)
    start_v = equity_curve[0][1]
    end_v = equity_curve[-1][1]
    total_return = end_v / start_v - Decimal(1) if start_v > 0 else Decimal("0")

    rets = [float(r) for r in period_returns]
    sharpe = _annualized_sharpe(rets, periods_per_year)
    sortino = _annualized_sortino(rets, periods_per_year)
    cagr = _cagr(rets, periods_per_year)
    mdd = _max_drawdown([float(v) for _, v in equity_curve])

    return BacktestResult(config_fingerprint, equity_curve, total_return, cagr, sharpe,
                          sortino, mdd, turnover_total, cost_total, n_rebalances, n_delisted,
                          benchmark_returns, caveats)


def _annualized_sharpe(rets: list[float], ppy: int) -> float:
    if len(rets) < 2:
        return 0.0
    sd = statistics.stdev(rets)
    if sd == 0:
        return 0.0
    return (statistics.mean(rets) / sd) * math.sqrt(ppy)


def _annualized_sortino(rets: list[float], ppy: int) -> float:
    if len(rets) < 2:
        return 0.0
    downside = [r for r in rets if r < 0]
    if not downside:
        return 0.0
    dd = math.sqrt(sum(r * r for r in downside) / len(rets))
    if dd == 0:
        return 0.0
    return (statistics.mean(rets) / dd) * math.sqrt(ppy)


def _cagr(rets: list[float], ppy: int) -> float:
    if not rets:
        return 0.0
    growth = 1.0
    for r in rets:
        growth *= 1.0 + r
    if growth <= 0:
        return -1.0
    years = len(rets) / ppy
    return growth ** (1.0 / years) - 1.0 if years > 0 else 0.0


def _max_drawdown(values: list[float]) -> float:
    peak = values[0]
    mdd = 0.0
    for v in values:
        peak = max(peak, v)
        if peak > 0:
            mdd = min(mdd, v / peak - 1.0)
    return mdd
```

- [ ] **Step 4: 통과 확인** → PASS
- [ ] **Step 5: Commit** — `git commit -m "feat: 백테스트 metrics BacktestResult·GuardReport·지표"`

---

## Task 9: engine — 리밸 루프·forward-return·폐지청산 (핵심)

**Files:** Create `src/stockpick/backtest/engine.py` · Test `tests/test_backtest_engine.py`

설계(BLOCKING 가드):
- 각 리밸일 t: `ports.load(as_of=t)` → `momentum_universe` → `rank_by_momentum`(constituents 교집합) → `strategy.weights`.
- **진입 = t 다음 거래일**(룩어헤드: 랭킹 t·진입 t+1). **청산 = 다음 리밸의 진입일**.
- 보유수익 = `full_series` 에서 [entry_day, exit_day] adjusted 비율. 폐지(`delisting_event` ∈ (entry,exit])면 `recovery_rate` 청산: ret = (last_adj/entry_adj)*recovery_rate - 1.
- 비용 = 리밸 turnover × cost_bps (회전분).

- [ ] **Step 1: 실패 테스트(결정적 합성 — 알려진 수익)**

```python
from datetime import date
from decimal import Decimal
from stockpick.rules._scan import PricePoint
from stockpick.backtest.config import BacktestConfig
from stockpick.backtest.engine import run
from stockpick.backtest.fakes import FakePriceSeriesPort, FakeUniversePort, StubIdentityResolver
from stockpick.backtest.strategy import EqualWeightTopN


def _series(ticker_prices: dict[str, list[tuple[date, str]]]) -> dict[str, list[PricePoint]]:
    return {t: [PricePoint(d, Decimal(p)) for d, p in pts] for t, pts in ticker_prices.items()}


def _cfg(**kw: object) -> BacktestConfig:
    base = dict(strategy_name="equal_weight_top_n", top_n=1, lookback_days=2, skip_recent_days=0,
                rebalance_freq="monthly", cost_bps=Decimal("0"),
                delisting_recovery_rate=Decimal("0"), group_by_exchange=False,
                start=date(2024, 1, 1), end=date(2024, 4, 30))
    base.update(kw); return BacktestConfig(**base)  # type: ignore[arg-type]


def test_single_winner_equity_grows_by_realized_return() -> None:
    # A 가 매월 +10%, top_n=1 → equity 가 A 수익 따라감(비용 0). 진입 t+1 규칙 확인.
    dates = [date(2024, m, d) for m, d in [(1, 2), (1, 31), (2, 1), (2, 29), (3, 1), (3, 29)]]
    prices = {"A": [(dates[0], "100"), (dates[1], "100"), (dates[2], "110"),
                    (dates[3], "110"), (dates[4], "121"), (dates[5], "121")]}
    port = FakePriceSeriesPort(_series(prices))
    uni = FakeUniversePort(listed={"A": date(2023, 1, 1)}, delisted={})
    res = run(_cfg(), price_port=port, universe_port=uni,
              identity=StubIdentityResolver({"A": "CIK_A"}), strategy=EqualWeightTopN())
    assert res.total_return > Decimal("0")  # 상승 추세 추종
    assert res.n_delisted_liquidations == 0


def test_delisting_realizes_recovery_rate() -> None:
    # A 보유 중 폐지, recovery_rate=0 → 그 구간 -100% 실현 → n_delisted_liquidations>=1
    dates = [date(2024, 1, 2), date(2024, 1, 31), date(2024, 2, 1), date(2024, 2, 29)]
    prices = {"A": [(d, "100") for d in dates]}
    port = FakePriceSeriesPort(_series(prices))
    uni = FakeUniversePort(listed={"A": date(2023, 1, 1)}, delisted={"A": date(2024, 2, 15)})
    res = run(_cfg(delisting_recovery_rate=Decimal("0")),
              price_port=port, universe_port=uni,
              identity=StubIdentityResolver({"A": "CIK_A"}), strategy=EqualWeightTopN())
    assert res.n_delisted_liquidations >= 1
```

- [ ] **Step 2: 실패 확인** → FAIL

- [ ] **Step 3: 구현**

```python
"""백테스트 엔진 — 단일 config 러너. rolling as_of 리밸런싱·forward-return·폐지청산·비용.

룩어헤드 BLOCKING: 랭킹은 ports.load(as_of=t)(<=t), 진입은 t **다음** 거래일(동시성 누설 차단).
생존편향 BLOCKING: 후보는 UniversePort.constituents(as_of=t) 교집합(가격파일 존재 아님). 보유 중
폐지(delisting_event)면 delisting_recovery_rate 로 청산(gap≠폐지 — 명시 이벤트로만).
"""

from __future__ import annotations

import bisect
import logging
from decimal import Decimal
from typing import TYPE_CHECKING

from ..rules.factors import momentum_universe
from ..rules.ranking import rank_by_momentum
from . import calendar, costs
from .metrics import compute_metrics

if TYPE_CHECKING:
    from datetime import date

    from ..rules._scan import PricePoint
    from .config import BacktestConfig
    from .metrics import BacktestResult
    from .ports import IdentityResolver, PriceSeriesPort, UniversePort
    from .strategy import Strategy

logger = logging.getLogger(__name__)

_PPY = {"monthly": 12, "quarterly": 4}


def run(
    config: BacktestConfig,
    *,
    price_port: PriceSeriesPort,
    universe_port: UniversePort,
    identity: IdentityResolver,
    strategy: Strategy,
) -> BacktestResult:
    """리밸 루프 → 자산곡선 → BacktestResult. 데이터량 무관(같은 코드, 더 많은 데이터)."""
    full = price_port.full_series()
    all_days = price_port.trading_days()
    in_range = [d for d in all_days if config.start <= d <= config.end]
    reb = calendar.rebalance_dates(in_range, freq=config.rebalance_freq)

    equity = Decimal(1)
    curve: list[tuple[date, Decimal]] = []
    period_returns: list[Decimal] = []
    turnover_total = Decimal(0)
    cost_total = Decimal(0)
    n_delisted = 0
    prev_weights: dict[str, Decimal] = {}

    for i, t in enumerate(reb):
        entry_day = _next_day(all_days, t)
        if entry_day is None:
            break
        exit_day = _entry_of_next(reb, i, all_days) or in_range[-1]

        ranked = _rank_at(config, price_port, universe_port, t)
        weights = strategy.weights(ranked, as_of=t)

        # 비용: 회전(turnover) = |new - old| 합. 진입 전 차감.
        turnover = _turnover(prev_weights, weights)
        cost_frac = costs.apply_cost_fraction(turnover, config.cost_bps)
        equity *= Decimal(1) - cost_frac
        turnover_total += turnover
        cost_total += equity * cost_frac

        # 보유수익(forward-return [entry_day, exit_day]) — 폐지 청산 포함
        pret, delisted = _holding_return(
            weights, full, entry_day, exit_day, universe_port, config.delisting_recovery_rate
        )
        n_delisted += delisted
        equity *= Decimal(1) + pret
        period_returns.append(pret)
        curve.append((exit_day, equity))
        prev_weights = weights

    return compute_metrics(
        curve, period_returns, periods_per_year=_PPY[config.rebalance_freq],
        turnover_total=turnover_total, cost_total=cost_total, n_rebalances=len(curve),
        n_delisted=n_delisted, benchmark_returns={},
        caveats=("골격: cik 미해소 가능·합성/제한 폐지·데이터 구간 짧음 — 결과 미검증(S6 전)",),
        config_fingerprint=config.fingerprint(),
    )


def _rank_at(config, price_port, universe_port, t):  # type: ignore[no-untyped-def]
    series = price_port.load(as_of=t)
    tradable = universe_port.constituents(as_of=t)
    series = {k: v for k, v in series.items() if k in tradable}  # survivorship: constituents 교집합
    scores = momentum_universe(series, as_of=t, lookback_days=config.lookback_days,
                               skip_recent_days=config.skip_recent_days)
    ticker_to_exchange = {k: _EX_PLACEHOLDER for k in series}
    return rank_by_momentum(scores, ticker_to_exchange, lookback_days=config.lookback_days,
                            top_n=config.top_n, group_by_exchange=config.group_by_exchange)


def _next_day(days: list[date], after: date) -> date | None:
    idx = bisect.bisect_right(days, after)
    return days[idx] if idx < len(days) else None


def _entry_of_next(reb: list[date], i: int, days: list[date]) -> date | None:
    return _next_day(days, reb[i + 1]) if i + 1 < len(reb) else None


def _turnover(old: dict[str, Decimal], new: dict[str, Decimal]) -> Decimal:
    keys = set(old) | set(new)
    return sum((abs(new.get(k, Decimal(0)) - old.get(k, Decimal(0))) for k in keys), Decimal(0))


def _holding_return(weights, full, entry_day, exit_day, universe_port, recovery_rate):  # type: ignore[no-untyped-def]
    total = Decimal(0)
    delisted = 0
    for ticker, w in weights.items():
        pts = full.get(ticker, [])
        entry_p = _price_on_or_after(pts, entry_day)
        if entry_p is None or entry_p <= 0:
            continue
        de = universe_port.delisting_event(ticker)
        if de is not None and entry_day < de <= exit_day:
            last_p = _price_before(pts, de) or entry_p
            ret = (last_p / entry_p) * recovery_rate - Decimal(1)
            delisted += 1
        else:
            exit_p = _price_on_or_before(pts, exit_day) or entry_p
            ret = exit_p / entry_p - Decimal(1)
        total += w * ret
    return total, delisted


def _price_on_or_after(pts: list[PricePoint], day: date) -> Decimal | None:
    for p in pts:
        if p.trade_date >= day:
            return p.adjusted
    return None


def _price_on_or_before(pts: list[PricePoint], day: date) -> Decimal | None:
    out = None
    for p in pts:
        if p.trade_date <= day:
            out = p.adjusted
        else:
            break
    return out


def _price_before(pts: list[PricePoint], day: date) -> Decimal | None:
    out = None
    for p in pts:
        if p.trade_date < day:
            out = p.adjusted
        else:
            break
    return out
```

(⚠️ 구현 중 `_EX_PLACEHOLDER` 는 `ticker_to_exchange` 를 실제로 `price_port`/`adapters` 의 `load_ticker_exchanges` 로 채우도록 교체 — 엔진이 exchange 매핑을 ports 에서 받게 시그니처 보강. 테스트는 단일거래소라 `Exchange.NASDAQ` 고정 헬퍼로 우선 통과시키고, adapters 연결 시 실데이터 매핑.)

- [ ] **Step 4: 통과 확인** → PASS
- [ ] **Step 5: Commit** — `git commit -m "feat: 백테스트 engine 리밸루프·forward-return·폐지청산"`

---

## Task 10: Sabotage 테스트 — 룩어헤드·생존편향 가드 봉인

**Files:** Test `tests/test_backtest_sabotage.py`

- [ ] **Step 1: 룩어헤드 sabotage**

```python
# 미래(exit 이후) 가격을 시계열에 추가해도 랭킹·결과 불변(랭킹은 as_of<=t 만).
def test_future_data_does_not_change_ranking_decision() -> None:
    ...  # 동일 config 로 (a)미래데이터 없음 (b)있음 → equity_curve 동일 단언
```

- [ ] **Step 2: 생존편향 sabotage**

```python
# 폐지종목 제거 vs 포함 → 지표 달라짐(생존편향이 결과에 영향 = 가드가 살아있음).
def test_removing_delisted_changes_metrics() -> None:
    ...  # FakeUniversePort 에 폐지 포함/제외 두 케이스 → total_return 다름 단언
```

- [ ] **Step 3: 실행·통과** → PASS
- [ ] **Step 4: Commit** — `git commit -m "test: 백테스트 sabotage 룩어헤드·생존편향 가드 봉인"`

---

## Task 11: benchmark — 등가중 유니버스

**Files:** Create `src/stockpick/backtest/benchmark.py` · Test `tests/test_backtest_benchmark.py`

- [ ] **Step 1~5(TDD):** `equal_weight_universe(price_port, universe_port, config) -> list[Decimal]`(기간수익) — 각 리밸 구간 constituents 전체 동일가중 수익. 룰 결과와 같은 유니버스·구간으로 공정 비교. 엔진의 `_holding_return` 재사용(weights=균등). engine.run 결과의 benchmark_returns 에 `{"EQUAL_WEIGHT_UNIVERSE": ...}` 채우도록 engine 연결. S&P500 은 historical constituents 필요 → caveat + 후속. 테스트: 단일종목 유니버스면 벤치=그 종목 수익.

- [ ] Commit — `git commit -m "feat: 백테스트 benchmark 등가중 유니버스"`

---

## Task 12: validation — IS/OOS·워크포워드·purge·decay

**Files:** Create `src/stockpick/backtest/validation.py` · Test `tests/test_backtest_validation.py`

- [ ] **Step 1: decay_ratio 엣지 테스트**

```python
from stockpick.backtest.validation import decay_ratio


def test_decay_ratio_normal() -> None:
    r = decay_ratio(is_sharpe=2.0, oos_sharpe=1.0, epsilon=0.1)
    assert r.decay_ratio == 0.5 and r.decay_warning is True and r.is_failed is False


def test_decay_is_failed_when_is_nonpositive() -> None:
    r = decay_ratio(is_sharpe=-0.5, oos_sharpe=1.0, epsilon=0.1)
    assert r.is_failed is True and r.decay_ratio is None  # 분모 무의미 → None


def test_decay_none_when_is_below_epsilon() -> None:
    r = decay_ratio(is_sharpe=0.05, oos_sharpe=0.04, epsilon=0.1)
    assert r.decay_ratio is None and r.is_failed is False  # 신호 미약 → 비율 산출 금지
```

- [ ] **Step 2: 실패 확인** → FAIL

- [ ] **Step 3: 구현(decay + walk_forward 골격)**

```python
"""과적합 가드 — IS/OOS 분할·워크포워드·purge gap·성과방어율(decay).

decay = OOS_sharpe/IS_sharpe. 단 IS_sharpe<=0 → 룰 기각(is_failed), |IS|<ε → 신호미약(None).
워크포워드: IS 창에서 선택·동결 → OOS 검증. 창 경계에 purge gap(=max(lookback,skip)) 삽입해
룩백이 OOS 를 침범하는 누수 차단. 1차는 분할·purge 로직(합성 검증), 통계 신뢰는 결제후.
"""

from __future__ import annotations

from .metrics import GuardReport


def decay_ratio(*, is_sharpe: float, oos_sharpe: float, epsilon: float = 0.1,
                warn_below: float = 0.5, purge_gap_days: int = 0) -> GuardReport:
    if is_sharpe <= 0:
        return GuardReport(is_sharpe, oos_sharpe, None, is_failed=True, decay_warning=False,
                           purge_gap_days=purge_gap_days,
                           notes=("IS Sharpe<=0 — 룰 IS 자체 실패, 기각",))
    if is_sharpe < epsilon:
        return GuardReport(is_sharpe, oos_sharpe, None, is_failed=False, decay_warning=False,
                           purge_gap_days=purge_gap_days, notes=("IS 신호 미약 — 비율 무의미",))
    ratio = oos_sharpe / is_sharpe
    return GuardReport(is_sharpe, oos_sharpe, ratio, is_failed=False,
                       decay_warning=ratio < warn_below, purge_gap_days=purge_gap_days)
```

`walk_forward(config, price_port, ..., n_folds, purge_gap_days)` — 구간을 IS/OOS fold 로 분할(경계 purge gap), 각 fold engine.run(IS·OOS) → decay_ratio. 합성 시계열로 "purge gap 이 룩백 누수 제거" 단위 테스트. 민감도는 인터페이스만(후속).

- [ ] **Step 4: 통과 확인** → PASS
- [ ] **Step 5: Commit** — `git commit -m "feat: 백테스트 validation IS/OOS·purge·decay"`

---

## Task 13: demo + __main__ — 무료 9종목 골격 시연

**Files:** Create `src/stockpick/backtest/demo.py`, `src/stockpick/backtest/__main__.py` · Test `tests/test_backtest_demo.py`(합성 base_dir smoke)

- [ ] **Step 1:** `demo.run_demo(base_dir)` — `ParquetPriceSeriesPort` + (골격) `FakeUniversePort`(가격파일 종목을 listed=구간시작·delisted=없음으로 — survivorship 한계 caveat 명시) + `StubIdentityResolver`(ticker=cik 폴백·caveat) + `EqualWeightTopN`. `engine.run` → BacktestResult 콘솔 출력(equity·CAGR·Sharpe·MDD·**data_caveats**). `rules/demo.py` 출력 패턴.
- [ ] **Step 2:** `__main__.py` → `raise SystemExit(run_demo(Path(os.environ.get("STOCKPICK_DATA_DIR","data/parquet"))))`.
- [ ] **Step 3:** 합성 Parquet 픽스처로 smoke 테스트(라이브 0).
- [ ] **Step 4:** 통과 확인 → PASS
- [ ] **Step 5: Commit** — `git commit -m "feat: 백테스트 demo·__main__ 골격 시연"`

---

## Task 14: 통합 검증 + work-history After + 문서 동기화

- [ ] **Step 1: 전체 게이트** — `docker compose exec -T app sh -c 'ruff check src tests && ruff format --check src tests && mypy && pytest -q'` → All pass
- [ ] **Step 2: 골격 end-to-end** — `docker compose exec app python -m stockpick.backtest` → BacktestResult + data_caveats 출력 확인(라이브 9종목 있으면, 없으면 ingest 먼저)
- [ ] **Step 3: 문서 동기화** — CLAUDE.md(backtest 빈패키지→구현 표기·meta.validated 사유 갱신 여부 검토)·PLAN_STATUS(진행현황 M2 백테스트 ✅·다음단계)·작업재개-RESUME·HOME(ADR-004)·webapp-conventions(BacktestPage 후속 명시). work-history After(검증출력·diff stat·SHA)+회고 채움.
- [ ] **Step 4: 최종 Commit** — `git commit -m "feat: M2 백테스트 엔진 골격 완료 — 문서 동기화·검증"`

> ⚠️ 결제후(S6 후속·별도 플랜): 실제 폐지 이벤트·IdentityResolver 실전(ticker_history)·S&P500 historical constituents·워크포워드/민감도 수치 신뢰·`/api/backtest`+BacktestPage 교체.

---

## Self-Review

- **Spec coverage**: 설계 7결정·red-team 6가드·1차범위 전부 Task 매핑됨(프레임워크→ADR Task1, config/fingerprint→T2, calendar→T3, costs→T4, strategy→T5, ports→T6, fakes/adapters→T7, metrics→T8, engine 룩어헤드/폐지→T9, sabotage→T10, benchmark→T11, validation/purge/decay→T12, demo→T13, 통합→T14).
- **Placeholder scan**: T9 `_EX_PLACEHOLDER`·T11/T12 walk_forward 본문은 "구현 중 보강" 명시 — 시그니처·테스트는 구체. 실행 시 exchange 매핑을 adapters.load_ticker_exchanges 로 연결(엔진 시그니처에 ticker_to_exchange 포트 추가) 필요 — T9 Step3 노트 참조.
- **Type consistency**: BacktestConfig 필드명·BacktestResult/GuardReport 필드가 T2/T8/T9/T12 전반 일치. PricePoint(trade_date·adjusted)·TopEntry(cik·ticker·score float) 실측 계약 준수.
- **알려진 보강점(실행자 주의)**: ① engine 의 ticker_to_exchange = ports 의 거래소 매핑으로 교체(T9) ② cost_total 누적은 차감 후 equity 기준 근사 — 정밀 회계 필요시 notional 기반으로 보강 ③ S&P500 벤치는 historical constituents 확보 전 미구현(caveat).
