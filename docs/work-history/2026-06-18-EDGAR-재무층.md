# 2026-06-18 EDGAR 재무층 슬라이스 — companyfacts 직접파싱 + ROE·P/B 팩터 (#재무-1)

- **유형**: 일반 구현 (플랜모드 승인)
- **관련 기획/이슈**: #2 cik resolver 후속 — cik 로 SEC 재무(companyfacts) 적재 → 멀티팩터(§4.2). SEC 무료·결제 무관
- **시작 시점 커밋**: `4065b08` → **완료 커밋**: 이 작업 마지막(완료 시 기입)

## 의도 (Why)

모멘텀 단일 팩터 → 밸류·퀄리티 멀티팩터 확장. SEC EDGAR companyfacts(무료·결제 무관)를 **직접 JSON
파싱**(소수 us-gaap concept)으로 적재, PIT(`filed`<=t) 안전하게 ROE·P/B 산출 → ranking factors 노출
(결합 안 함·§9-2). edgartools 미사용(heavy dep·~15필드 미검증 회피 — ADR-005). ⚠️ 재무팩터 노출해도
`meta.validated=false` 불변(검증은 가격 결제+S6 후).

## 계획 (승인 플랜 백업)

브레인스토밍 확정: 직접 JSON 파싱(소수 concept) / 데이터층+소수 팩터(P/B·ROE) end-to-end.

Phase0 ⚠️ api-spec BLOCKING: tech-researcher가 `docs/apis/sec-edgar/companyfacts.json` 캡처(응답
`{facts.us-gaap.{Concept}.units.{USD|shares}[].{end,val,filed,fy,fp,form}}`·PIT=filed·concept 확정) +
ADR-005(직접파싱·ADR-002 edgartools 개정).

파일: `types.FinancialFact`(cik·concept·fiscal_period·disclosed_at·value) · `data/edgar.py`(+fetch_companyfacts/
store/load·`__main__` 데이터셋 9 cik만) · `rules/_financials.py`(PIT load·latest_as_of disclosed_at<=as_of) ·
`rules/factors.py`(+financial_factors ROE=NetIncome/Equity·P/B=price/(Equity/shares)) · ranking 라우트
factors enrich(rank 불변) · 테스트(httpx 모킹).

가드: 룩어헤드=disclosed_at(filed)<=as_of(end 아님·재무 공시 시차) / Decimal / 분모0·결측 None /
결합·가중치 금지(§9-2) / validated=false 불변.

## Before — 수행 전 실측

- `types.py:96-100` = Financial 설계 주석만(미구현). `src/stockpick/data/` = edgar(company_tickers만)·tiingo·eodhd 등.
- `docs/apis/sec-edgar/` = company-tickers.json·_index(companyfacts forward_pointer만·미캡처).
- `rules/factors.py` = momentum 단일. ranking factors = {"momentum":...}.
- edgartools 미설치. `.env` EDGAR_IDENTITY 세팅·ticker_cik.json 라이브 10,414건 적재됨(#2).
- 테스트 197 passed.

## After — 수행 후 실측

Step 0~6 완료. 20파일 +1432/-44(`git diff 4065b08..HEAD`). 커밋 6개(Step0=02829c5 docs, Step1=fb37de8,
Step2=fe7394a, Step3=c7afc36, Step4=fcb2ef0, Step5=d9f20c1).

**구현**:
- `docs/apis/sec-edgar/companyfacts.json` 캡처(companyconcept 3종 라이브 실측 — fact 스키마
  end/val/accn/fy/fp/form/start?/frame? 확정·PIT=filed·NetIncome 연분기 혼재·dei shares). _index 2섹션 승격.
- `ADR-005` 직접 JSON 파싱(edgartools 미사용 — ADR-002 부분 개정). HOME·api-spec-reference 갱신.
- `types.FinancialFact`(cik·concept·fiscal_period·period_end·disclosed_at·value:Decimal, 자연키 4축).
- `data/edgar`: fetch_companyfacts(소수 concept 직접 파싱)·store/load_financials·fetch_dataset_financials
  (데이터셋 cik만·10req/s·실패 집계)·`__main__ financials` 모드. `storage.list_dataset_tickers`.
- `rules/_financials.latest_as_of`(PIT disclosed_at<=as_of·정정 최신·annual_only) — 재무 룩어헤드 단일 출처.
- `rules/factors.financial_factors`(ROE=연간 NetIncome/Equity·P/B=price*shares/equity, 분모<=0·결측 None).
  `_scan.load_close_as_of`(명목 raw close — P/B 일관). ranking 라우트 factors enrich(rank 불변·§9-2).

**검증**: `ruff+ruff format+mypy(strict)+pytest` 전부 통과. 테스트 **197→231(+34)**: edgar +17(companyfacts
파싱·PIT·연분기·결측·실패집계·dataset cik만)·financials +8(PIT sabotage 미래공시 제외 봉인)·factors +7
(ROE/PB 정확값·적자 음ROE·0분모/결측 None·룩어헤드)·api +2(enrich·룩어헤드). 라이브 0(httpx 모킹).

**라이브 검증(무료·결제 무관)**: `python -m stockpick.data.edgar financials` → 데이터셋 9종목 cik companyfacts
fetch → **4571 fact 저장**. `/api/ranking`(as_of=2026-06-16) factors 실값 확인:
| ticker | ROE | P/B | 비고 |
|---|---|---|---|
| AAPL | 1.519 | 59.6 | 자사주매입로 낮은 자기자본 → 높은 ROE/PB(실제값) |
| NVDA | 0.763 | 31.9 | FY2026(1월 결산) |
| MSFT | 0.297 | 8.52 | |
| GOOGL/META | 0.318/0.278 | **None** | dei 주식수=0(다중 클래스 dimensional) → P/B 불가 |
| JNJ | **None** | **None** | StockholdersEquity 18 fact 전부 10-Q(연간 0) — 변형 태그 사용 |
| AMZN/JPM/XOM | 0.189/0.157/0.111 | 6.44/2.45/2.27 | |
validated=false 불변 확인. rank=모멘텀(GOOGL 1위 — 재무 결합 안 함). 미해소→키 생략(조용한 누락 0).

## 비교/회고

- **의도 대비 달성도 100%**: 모멘텀 단일 → 밸류·퀄리티 멀티팩터 노출 end-to-end. 무료 EDGAR 로 결제 전
  실데이터 검증 완료. 7/9 ROE·5/9 P/B 실값, validated=false 불변.
- **None 케이스 = 데이터 이슈(버그 아님, debugging-discipline 3분류)**: JNJ 연간 equity 는 변형 태그
  (`StockholdersEquityIncludingPortionAttributableToNoncontrollingInterest` 추정), GOOGL/META 주식수는
  다중 클래스 dimensional 이라 flat companyfacts 미노출. 파서는 정상(결측→None·추측 0). 이게 ADR-005
  "edgartools 광범위 정규화 후속" 의 실증 근거 — 슬라이스 한계를 honestly 드러냄.
- **룩어헤드 BLOCKING**: disclosed_at(filed)<=as_of 단일 출처(_financials)·sabotage 테스트 2중(unit+api).
  period_end(end) 아님 — 공시 시차 누설 차단.
- 후속: [ ] StockholdersEquity 변형 태그 폴백(JNJ) · [ ] 다중클래스 주식수 합산(GOOGL/META) ·
  [ ] TTM(4분기합) ROE · [ ] edgartools 광범위 정규화 · [ ] PG financial · [ ] 멀티팩터 결합(백테스트
  결정·§9-2) · [ ] ticker_history
