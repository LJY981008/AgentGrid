# 2026-06-18 EDGAR 재무층 슬라이스 — companyfacts 직접파싱 + ROE·P/B 팩터 (#재무-1)

- **유형**: 일반 구현 (플랜모드 승인)
- **관련 기획/이슈**: #2 cik resolver 후속 — cik 로 SEC 재무(companyfacts) 적재 → 멀티팩터(§4.2). SEC 무료·결제 무관
- **시작 시점 커밋**: `4065b08` → **완료 커밋**: 이 작업 마지막(완료 시 기입)

## 의도 (Why)

모멘텀 단일 팩터 → 밸류·퀄리티 멀티팩터 확장. SEC EDGAR companyfacts(무료·결제 무관)를 **직접 JSON
파싱**(소수 us-gaap concept)으로 적재, PIT(`filed`<=t) 안전하게 ROE·P/B 산출 → ranking factors 노출
(결합 안 함·§9-2). edgartools 미사용(heavy dep·~15필드 미검증 회피 — ADR-005). ⚠️ 재무팩터 노출해도
`meta.validated=false` 불변(검증은 가격 결제+S6 후).

## 계획 (승인 플랜 백업)

브레인스토밍 확정: 직접 JSON 파싱(소수 concept) / 데이터층+소수 팩터(P/B·ROE) end-to-end.

Phase0 ⚠️ api-spec BLOCKING: tech-researcher가 `docs/apis/sec-edgar/companyfacts.json` 캡처(응답
`{facts.us-gaap.{Concept}.units.{USD|shares}[].{end,val,filed,fy,fp,form}}`·PIT=filed·concept 확정) +
ADR-005(직접파싱·ADR-002 edgartools 개정).

파일: `types.FinancialFact`(cik·concept·fiscal_period·disclosed_at·value) · `data/edgar.py`(+fetch_companyfacts/
store/load·`__main__` 데이터셋 9 cik만) · `rules/_financials.py`(PIT load·latest_as_of disclosed_at<=as_of) ·
`rules/factors.py`(+financial_factors ROE=NetIncome/Equity·P/B=price/(Equity/shares)) · ranking 라우트
factors enrich(rank 불변) · 테스트(httpx 모킹).

가드: 룩어헤드=disclosed_at(filed)<=as_of(end 아님·재무 공시 시차) / Decimal / 분모0·결측 None /
결합·가중치 금지(§9-2) / validated=false 불변.

## Before — 수행 전 실측

- `types.py:96-100` = Financial 설계 주석만(미구현). `src/stockpick/data/` = edgar(company_tickers만)·tiingo·eodhd 등.
- `docs/apis/sec-edgar/` = company-tickers.json·_index(companyfacts forward_pointer만·미캡처).
- `rules/factors.py` = momentum 단일. ranking factors = {"momentum":...}.
- edgartools 미설치. `.env` EDGAR_IDENTITY 세팅·ticker_cik.json 라이브 10,414건 적재됨(#2).
- 테스트 197 passed.

## After — 수행 후 실측 (완료 시 기입)

> **진행중** — Step 0~6 완료 시 채움(검증·라이브 roe/pb·변경규모·커밋).

## 비교/회고 (완료 시 기입)

> **진행중**

- 의도 대비 달성도:
- 후속: [ ] TTM(4분기합) ROE · [ ] edgartools 광범위 정규화 · [ ] PG financial · [ ] 멀티팩터 결합(백테스트 결정·§9-2) · [ ] ticker_history
