# 2026-06-16 M0 — 도메인·스택 전환 (MCP 레지스트리 → 한국주식 분석)

- **유형**: 하네스/인프라 (대규모 구조 전환)
- **관련 기획**: [[plans/stock-1st_plan]] §1(전환 선언)·§8(M0), [[plans/PLAN_STATUS]]
- **시작 시점 커밋**: `cadec2b` → **완료 커밋**: 이 엔트리를 포함한 단일 M0 커밋 (drift 커플링상 1·2차를 한 커밋으로 — git log 정본)

## 의도/목적

레포 도메인을 MCP 신뢰성 레지스트리 → 개인 투자용 한국주식 분석으로 전환. 사용자 강조: **"내용물은 이전 기획 잔재가 전혀 없어야"**. 골격(하네스·git·볼트·work-history 규약)은 유지, 스택(Java/Spring+Next)·도메인 콘텐츠는 전량 교체. 데이터 결정과 독립적으로 환경부터 깨끗이.

## 계획 (전수 스캔 근거 — 추측 아님)

전수 grep 결과 잔재 ~35파일 + 디렉토리 3개. 2단계 커밋:

**1차 (이 커밋)**: 구 스택 삭제 + Python 골격 + rules 재작성
- 삭제: `backend/`(Spring 전체) · `frontend/`(Next mock·신뢰성 등급 UI) · `infra/monitoring/`(Spring Actuator 전제)
- Python 골격: `pyproject.toml`(uv) + `src/` 모듈 레이아웃 최소(data/rules/backtest 예정)
- rules: backend-conventions(Java)→python-conventions / frontend-conventions(Next)→webapp-conventions(PWA) / logging-rules(SLF4J)→Python logging
- compose: RabbitMQ 제거, PG 유지

**2차 (다음 커밋)**: 하네스 도메인 교체
- agents 11종 description(Spring/Next→Python/주가) + agent-memory 재시드
- 훅: post-work-check(gradlew/npm→ruff/mypy/pytest)·harness-drift-check(경로)·session-start(watchPaths)·settings(권한)
- skills(debugging-discipline 함정), CI/vscode, CLAUDE.md 3종·AGENTS·README

## Before — 수행 전 실측

- 디렉토리: backend/(Spring Boot 4.1, 도메인 코드 0·스캐폴드만) · frontend/(Next 16 mock 4화면) · infra/monitoring/(Prometheus·Grafana)
- 하네스: 훅 9·rules 3·agents 11·skills 2 — 전부 Java/Next/MCP 도메인 박힘
- 잔재 키워드 매칭: 도메인(신뢰성·등급·레지스트리·제출·Outbox) ~20파일 / 스택(Spring·gradle·Next·Testcontainers·RabbitMQ) ~40파일
- 커밋 10개 (구 도메인 히스토리 — 보존)

## After — 수행 후 실측

**삭제 디렉토리** (3): `backend/`(Spring 전체) · `frontend/`(Next mock — node_modules 633M 포함 untracked까지 `rm -rf`) · `infra/monitoring/`(Prometheus·Grafana).

**Python 골격 검증**:
- `python3 --version` → 3.12.3 / `ruff`·`pytest` 설치됨, `mypy`·`uv` 미설치(post-work-check 가 tool-conditional 로 skip — 설치 시 자동 활성)
- `ruff check src tests` → **All checks passed** (S101 은 `tests/**` per-file-ignore — pytest assert 정상 패턴)
- `ruff format --check` → 6 files formatted
- `PYTHONPATH=src pytest tests/` → **계약 테스트 4/4 PASS** (Market enum / Stock.delisted_at nullable=생존편향 / TopEntry.factors / DailyBar.value nullable)

**하네스 잔재 0 확인** (재스캔 2회):
- 1차 전수 grep → 후보 24파일, 라인 단위 육안 분류 → **진짜 잔재 3건 적발 후 수정**: ①`tech-researcher.md` frontmatter description 이 구 스택(Boot/Next/Testcontainers/springdoc/Resilience4j) 잔존 — 본문만 고치고 frontmatter 누락분 ②`settings.json` claudeMdExcludes `build`/`.gradle`(Java) → `.venv`/`__pycache__`/`.mypy_cache`/`.ruff_cache`/`.pytest_cache` ③`tech-researcher` 메모리 과도기 표현("Python 예정"·"전환 과도기") → 현재형
- 2차 재스캔(부정맥락 제외) → 남은 매칭 4건 전부 **정당**: 전환 이유 설명·미해결 이월(RabbitMQ/Outbox 재검토)·"데이터 신뢰성"(주식 도메인 정당 용어). **구 도메인 코드/설정 잔재 0**
- agent-memory 13파일 → `git rm -rf` 후 3개로 재시드(python-expert 신규 / product-planner·tech-researcher pivot 보존 — 구 도메인 회귀 가드)

**훅 회귀**: `.claude/hooks/tests/run.sh` → **PASS=38 FAIL=0**

**drift**: 단일 커밋에 rules·agents·hooks·settings ↔ CLAUDE.md, src ↔ work-history 동시 포함 → harness-drift-check 통과 조건 충족

**변경 규모**: 118 files changed, **+1099 / −10283** (구 도메인 대량 삭제). 단일 M0 커밋(git log 정본).

## 비교/회고

- **단일 커밋 강제의 정당성 입증**: drift-check 가 rules/agents/hooks↔CLAUDE.md, src↔work-history 를 커플링 → 중간 커밋이 차단됨. M0 를 의도적으로 한 커밋에 묶은 게 옳았다(분할 시 매 커밋 drift 실패).
- **잔재 청소의 함정**: "본문은 고쳤는데 frontmatter description 누락" — 에이전트 description 은 자동 매칭에 쓰이는 실효 메타라 누락 시 다음 세션에 구 도메인으로 라우팅됐을 것. **라인 단위 grep 육안 분류**가 파일 단위 체크보다 정확했다.
- **부정맥락 필터**: "폐기/전환/아님" 맥락의 구 키워드는 잔재가 아니라 가드(회귀 방지 기록)다. 무조건 삭제가 아니라 의미 분류가 필요했다.
- 후속: M1 데이터 수집·저장 (사용자 KRX OpenAPI 키 발급 후 — db-architect 시계열 스키마 + python-expert 파이프라인). M1(데이터 신뢰성) 통과 없이 M2 백테스트 금지.
