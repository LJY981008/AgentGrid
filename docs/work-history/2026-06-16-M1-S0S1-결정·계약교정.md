# 2026-06-16 M1 S0-S1 — 결정 확정·계약 정밀도 교정

- **유형**: 일반 구현
- **관련 기획/이슈**: [[plans/M1-데이터파이프라인]] §4 S0-S1 / [[plans/stock-1st_plan]] §8 M1 / 미해결 #1·#4·#8 / [[ADR-001-마이그레이션-도구-alembic]]
- **시작 시점 커밋**: `7d60ab9` → **완료 커밋**: 이 커밋 (git log 정본)

## 의도/목적 — 왜 이 작업을 하나

KRX OpenAPI 키 확보로 M1 언블록. 코드 착수 전, 금융 BLOCKING(생존편향·룩어헤드·수정주가·재현성)을 추측 없이 정하기 위해 실측 스코핑 워크플로우(tech-researcher·db-architect·product-planner) 실행 → 사용자 결정 확정. 이 작업이 끝나면 M1 데이터 파이프라인(S2~)을 확정된 스키마·소스·게이트 위에서 시작할 수 있다.

## 계획

S0(결정 기록) + S1(도메인 계약 정밀도 교정)을 한 커밋:
- S0: ADR-001(alembic) + M1 스펙 문서(스코핑 종합) + PLAN_STATUS/HOME 동기화
- S1: `types.py` DailyBar 가격 `float`→`Decimal`(정밀도 BLOCKING), `adj_factor` 추가(원주가+조정계수 모델), 계약 테스트 갱신

사용자 결정(상세 설명 후 재확인): 신뢰성 게이트 = **넓게 수집+품질 꼬리표 / 표준·엄격 임계는 M2 민감도분석 gap**(사용자 발안, 과적합 경고 부가) · 폐지 fallback = 확보분+누락고지+미달시 차단 · alembic.

## Before — 수행 전 실측

- `src/stockpick/types.py` DailyBar 가격 4필드 = `float` (부동소수 오차 → 수익률 왜곡 위험, 스키마 numeric 요구와 불일치)
- `docs/decisions/` 비어있음(첫 ADR), 마이그레이션 도구·의존성 미정
- 미해결 질문 #1(소스)·#4(DB)·#8(폐지 fallback) 미결
- 계약 테스트 4/4 PASS(가격 int 리터럴)

## After — 수행 후 실측

- 검증: `ruff check src tests` → All checks passed / `ruff format --check` → 6 files formatted / `PYTHONPATH=src pytest` → **4/4 PASS**(DailyBar Decimal·adj_factor=1 검증 추가). mypy·uv 미설치(tool-conditional skip)
- 변경: `types.py`(가격 Decimal+adj_factor, docstring 모델 갱신), `test_contract.py`(Decimal), ADR-001 신규, M1 스펙 신규, PLAN_STATUS/HOME/INDEX 동기화
- 커밋: 단일 커밋(git log 정본)

## 비교/회고

- 의도 대비: M1 착수 기반(소스·스키마·게이트·도구) 전부 확정. 추측 0 — 모든 소스 시그니처·커버리지 실측(워크플로우 243k 토큰, 4에이전트).
- 계획과 달라진 것: 신뢰성 게이트가 '단일 임계 선택' → **사용자 발안으로 '넓게 수집+M2 민감도 gap 분석'으로 격상**. 더 강건한 설계(수집 단계 데이터 미파기 → M2에서 표준·엄격 비교 가능). 과적합 함정(gap으로 임계 골라잡기 금지)을 BLOCKING 으로 명시.
- adj_factor 를 S1 에서 계약에 포함: 원주가+조정계수 모델이 확정(D5)됐으므로 추측 아님. source/ingested_at·is_etf_or_spac 등 저장 메타는 S2 저장계층에서.
- 후속: [ ] S2 종목마스터 적재(폐지 합집합 UPSERT) — 단 alembic+의존성은 pip 실측 고정·devops 조율 후. [ ] 코드 전 미실측 API 표면 실호출 확인(FDR StockListing 컬럼 등)
