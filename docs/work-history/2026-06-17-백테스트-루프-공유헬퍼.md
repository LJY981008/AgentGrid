# 2026-06-17 백테스트 리밸 루프 공유 헬퍼 추출 (#5 리팩터)

- **유형**: 리팩터 (동작 불변 — 테스트가 안전망)
- **관련 이슈**: 최종 코드리뷰(opus) Important — engine/benchmark 리밸 루프 골격 복붙 → 드리프트 리스크
- **시작 시점 커밋**: `712767c` → **완료 커밋**: 이 커밋

## 의도 (Why)

`engine.run` 과 `benchmark.equal_weight_universe` 가 리밸 루프 골격(in_range·rebalance_dates·last_day·
진입 t+1·청산·entry>exit 스킵·앵커 시드)을 **각자 복붙**으로 갖고 있었다(공유는 `_next_day`·
`_holding_period_return` 둘뿐). 한쪽 회계 규칙 수정 시 다른 쪽 누락 → 두 자산곡선이 다른 회계로 계산
→ "공정 비교" 조용히 깨짐(돈 걸린 드리프트, 테스트 미포착). 루프 경계를 단일 출처로 추출.

## Before

- engine.run·benchmark 둘 다 동일 루프 스캐폴드 복제(약 15줄씩). `_next_day` 는 engine 사유·benchmark 가 import.
- 테스트 195 passed.

## After

- `calendar.holding_periods(all_days, *, start, end, freq) -> RebalancePlan(anchor, periods[(t,entry,exit)])`
  신설 — 진입 t+1·청산·스킵·앵커를 **회계 경계 단일 출처**로 캡슐화. `_next_day` 를 calendar 로 이전.
- engine.run·benchmark.equal_weight_universe 둘 다 `plan = calendar.holding_periods(...)` 후
  `for t, entry, exit in plan.periods:` 로 통일. 복제 루프 골격·engine `_next_day`·`import bisect` 제거.
- **동작 100% 보존**: `pytest` **197 passed**(195→197, +2 holding_periods 단위테스트). engine/benchmark/
  sabotage(룩어헤드 불변·폐지 -1 정확)/validation 전부 green = 자산곡선·지표 불변 입증. ruff·mypy·훅 38.
- 변경: `calendar.py`(+holding_periods·_next_day·RebalancePlan) · `engine.py`(루프 교체·_next_day 제거) ·
  `benchmark.py`(루프 교체) · `test_backtest_calendar.py`(+2).
- 완료 커밋: 이 커밋.

## 회고

- 의도 대비 달성: 100% — 회계 경계 단일화로 드리프트 리스크 제거. seam(인접 구간 청산=다음 진입)·
  앵커 동작 정확 보존(테스트가 증명).
- 순수 리팩터 — 새 기능·behavior 변경 0이라 브레인스토밍 생략, 기존 테스트(특히 engine 결정성·
  sabotage 정확값)를 회귀 가드로 사용.
- 후속(범위 밖): TickerHistoryResolver · EDGAR 재무층 · validated=true(결제+S6).
