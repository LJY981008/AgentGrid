# 2026-06-25 백테스트 방법론 전면 교정 — momentum 공정 측정 → validated 판정

- **유형**: 플랜모드 승인(다전문가 토의·2회 리뷰). [[2026-06-24-A1p2-상한garbage-정제]] 후속(2차 폭발 해소).
- **시작 커밋**: (직전 HEAD 551b438/6e0b0ea) → **완료 커밋**: (단계별)
- **승인 플랜 전문 백업**: `/home/code/.claude/plans/pro-3-5-twinkling-adleman.md`(활성 플랜·"백테스트 방법론 전면 교정") — 아래 요약.

## 의도/목적

A1p2(sentinel $1M 정제)로 핵폭발(−10^80)은 72자릿수 제거됐으나, 게이트 재실행 결과 **2차 폭발 잔존**(oos_excess e7). 3 전문가(미국주식 퀀트·구현·적대비판) 독립 수렴: 폭발 지배원인 = microcap penny(트리거)가 아니라 **측정 셋업(증폭기)** — `top-5(w=0.2) × per-ticker +19 cap × 산술복리 × 등가중-전체유니버스 벤치`. 유동성 필터만 = band-aid. 목표 = momentum 을 **표준 퀀트 셋업으로 공정 측정** → 정직한 validated 판정(fail이면 그때 다팩터 피벗 정당). **방법론 ALL 동결 후 게이트 1회**(pre-registration 회복).

## 승인 플랜 요약 (전문 = 플랜파일)

- **Phase 0(진단·게이트 전)**: 0a per-period pret 분포(증폭기 vs microcap·cap 비대칭·clip-hit 사후수익) · 0b 빠른 개발경로(축소 config·분 단위) · 0c PIT 시총 가용성(SEC EDGAR shares 커버율) · **0d 데이터 신뢰구간**(EODHD 폐지 완비 시작연도→기간 동결·"1962~2026" 폐기·🔴 BLOCKING).
- **Phase 1(ADR-010 일괄 동결·R1 방화벽=진단은 방법 종류만·임계는 외부원칙)**: ①수익처리(per-ticker +19 cap 폐기→simple return clip ±100%→로그합산·root) ②포트=top decile(상위10%·가변) ③lookback 12-1(252/21) ④벤치 동일비용+유동성+시총/ADV가중 ⑤유니버스 PIT 필터(close≥$5+ADV20≥$1M+시총하한+종목유형 제외) ⑥G-3 재동결(ADR-009 amend) ⑦R2 측정가능성 게이트(|worst_oos_excess|≤X).
- **Phase 2**: 9-Task TDD(config·LiquidityPort·cache volume·engine/bench 대칭·sabotage·signature·ranking-API (c)사후필터) → 빠른경로 sanity → 신뢰구간 게이트 1회.
- **Phase 3**: R2 통과 → G-1~G-8 → validated 결정.

**전문가 reservation 반영**: R1 방화벽(C2 정신)·R2 측정게이트·R3 clip 우측꼬리 점검·R4 decile≠Top5 다리(critic) + 0d 신뢰구간·decile=비율·lookback 12-1·clip 순서·fallback ADV가중(tech-researcher). 양쪽 ACCEPT-WITH-RESERVATIONS.

## Before (실측 — 정제후 게이트 2026-06-25·s6_gate_result.json)

현 셋업(top-5 등가중·월간·lookback126/skip21·+19 cap·등가중-전체 무비용 벤치·1962~2026·10폴드):
- **판정 FAILED → validated=false**. criteria: G-1✅·G-2❌(fold0=0.497·fold9=0.413<0.5)·G-3❌·G-4✅·G-5✅(62.9%·n=14)·G-6❌·G-7✅·G-8✅.
- **oos_excesses** = [0.011, −0.795, 45.58, 169.44, 94467.27, 20275.68, 3479.33, 27925386.6, **84781977.3**, −47.007] — 8/10 폴드 e2~e7 폭발(정상이면 O(1)).
- **fold_decays** = [0.497, 0.509, 1.485, 1.564, 2.304, 1.560, 1.329, 1.985, 1.955, 0.413] — 7/10 >1(OOS>IS=비정상).
- sensitivity 5/10/15bps 전부 0.4129 동일(비용이 폭발 magnitude 에 묻힘).
- 데이터: 98,235,517행·50,106 ticker·1962~2026(A1p2 정제후·sentinel 0).

## After (단계별)

### Phase 0c/0d 진단 결과 (2026-06-25·실측·debugging-discipline)

데이터 연도별 밀도(distinct ticker): 1962=27·1970=30·1980=556·1990=1,481·1995=2,814 → **2000=10,222**(3.6배 급증)·2010=17,741·2020=25,549.
폐지(delisted_at) 분포: pre-1995≈**0**·1995~99=1,011·2000~04=2,657·2015~19=6,808·2020~24=13,679 (총 31,868=63.5%).
SEC financials: financials.json = **9 cik만 수집**(4,571 fact·StockholdersEquity 1,678·NetIncomeLoss 2,412·SharesOutstanding 481·shares 보유 7 cik). ticker_cik 매핑 10,414.

**→ 동결 결정(Phase 1 단순화)**:
- **0d 기간 → 2000~2026 동결**: pre-2000 = 생존자 편중(<3k 티커·폐지 기록 ≈0) → 생존편향 부활. "1962~2026" 폐기. 신뢰구간 = 폐지 완비 시작 ~2000.
- **0c 시총 필터 → 불가 → ADV 프록시 확정**: SEC shares 커버율 ≈0.01%(7/50,184·financials 자체가 9 cik 파일럿). **시총가중 벤치·시총하한 둘 다 불가** → 벤치 = 동일비용 유동성-등가중(또는 ADV가중)·유니버스 size 컷 = ADV20≥$1M 프록시 단독. (R1 방화벽: 이건 데이터 가용성 사실이지 결과-튜닝 아님.)
### Phase 0a 진단 결과 (2026-06-25·diag_0a.py·계측 없이 equity_curve 유도·결과불변)

현 셋업(top-5·+19cap·2000~2026) 1패스: 318 리밸·**total_return=2.2e20**(기간 동결만으론 폭발 잔존=증폭기 증거).
- pret 분포: >+380% **20개(6.3%)** · +100~380% 25 · 정상(±20%) 186(58.5%) · <−20% 63.
- **top pret 클러스터 = cap-hit 시그니처**: +3.85/+3.86/+3.87 다수 = `0.2×19=3.8`(1 cap-hit) / +7.48/+7.53/+7.65/+7.82 = `0.4×19=7.6`(2 cap-hit). pret 가 `w×cap` 배수에 정확히 박힘.
- 분산 집중: 상위 1% 기간=분산 23% · 상위 5%=54% · **상위 10%=84%**.

**→ 증폭기(C1) 결정적 확정**(critic 산수 정확 일치): 폭발 = per-ticker +19 cap × top-5(w=0.2) × 복리. cap-hit = ret≥+1900%/월(정상 momentum 알파 아님). microcap **빈도** 아님(소수 기간 지배). ±100% clip 시 1 cap-hit pret +20%(×4.85→×1.2)=폭발 소멸 → **Phase 1.1 수익처리 재정의 = root fix 입증**. R3(clip 우측꼬리) 완화(+1900%는 정당 알파 아님).
- 0b 빠른경로: walk_forward 이미 n_folds 파라미터화·canonical_gate_config start/end 받음 → 게이트 CLI env 오버라이드만(Phase 2 통합).

**Phase 0 완료** — 다음 = Phase 1 일괄 동결(ADR-010).

### Phase 1 + Phase 2 진행 (ADR-010 + 구현 TDD)

- **Phase 1**: [[ADR-010-백테스트방법론-표준교정-동결]] 작성 — 기간 2000~2026·수익처리 ±100% clip·top decile·lookback 12-1·동일비용 등가중 벤치·유동성 PIT 필터(시총하한 없음·0c)·R1 방화벽·R2 측정게이트(|worst_oos_excess|≤10)·R4 decile≠Top5·G-3 집계 전-fold 유지(보수).
- **Phase 2-1 수익처리 root fix**(커밋 5bcb48e): `period_return_cap` 19→1.0. **실증 검증**(동일 셋업·cap만): total_return **2.2e20 → −1.0**(폭발 소멸)·pret>+380% **20→0개**·최대 +62%(×1.62)·상위10% 분산 84%→45%. 증폭기=root 최종 입증(cap 한 값으로 e20 붕괴). −1.0=top-5·전유니버스·필터전 momentum 26년 손실(측정가능).
- **Phase 2-2 유동성 config 3필드**(커밋 be0cff2): min_price_floor($5)·min_adv_dollar($1M)·adv_window_days(20)·fingerprint+검증.
- **Phase 2-3 LiquidityPort + SQL + cache volume**(커밋 267e5ea): `query_liquid_tickers`(단일 SQL·ADV·룩어헤드)·`DuckDBLiquidityPort`·`FakeLiquidityPort`·`_select_liquidity_port`·build_cache volume(momentum bit-identical). TDD 가격/ADV/window/룩어헤드.
- 남은 Phase 2: decile 전략·engine/벤치 유동성 대칭 배선·벤치 동일비용·signature 동결·ranking (c)·기간 동결 → 빠른경로 sanity → 게이트 1회.

⚠️ 정정: 0c 1차 측정 "shares 0%"는 코드버그(financials=list 인데 dict 순회). 재측정으로 정정(debugging-discipline).

### Phase 2-4 decile 전략 + config 필드 (TDD·커밋 후속)

ADR-010 #3 top decile 구현 1단계 — 포트폴리오 분산을 고정 top_n 에서 **유동 후보 상위 decile(가변)** 로.
- **config**: `portfolio_pct: Decimal|None`(기본 None=고정 top_n·decile opt-in)·`decile_min_holdings: int=20`(초기/소형 floor). 둘 다 `__post_init__` 검증(0<pct≤1·min>0)·fingerprint(asdict→_canonical None 통과) 포함 → 동결 우회 차단(top_n 과 다른 룰 정체성).
- **strategy**: `TopDecileEqualWeight(*, pct, min_holdings)`·name=`top_decile_equal_weight`. 입력 `ranked`=유동 필터 후 momentum 랭킹 전 후보(rank 오름차순). 보유수=`clamp(max(min_holdings, ceil(pct×N)), 1, N)`. 분모 N=후보수(M1·전체 유니버스 아님). 평면 랭킹 전제(group=False·게이트 정규값).
- **TDD**: strategy 6케이스(decile 10%·floor binding·ceil 올림·N cap·빈입력·name 구분) + config 6케이스(fingerprint pct/min 민감·None 기본·범위검증). RED(ImportError)→GREEN. ruff/mypy clean(91 files).
- ⚠️ **미배선**: 엔진/벤치/signature 는 Phase 2-5~7. 이 커밋은 신규 필드+클래스만(기존 경로 동작 불변·EqualWeightTopN/top_n 그대로).

### Phase 2-5/2-6 engine·bench 유동성 대칭 배선 + 벤치 동일비용 (TDD·커밋 후속)

ADR-010 #5·#6 — 유동성 PIT 필터를 engine·bench 대칭 배선하고 벤치를 동일비용화.
- **engine**: `run`·`_rank_at` 에 `liquidity_port: LiquidityPort` **필수 kw**(누락=mypy 차단·조용한 skip 금지). `tradable &= liquidity_port.liquid_tickers(as_of=t, candidates=tradable)`(룩어헤드 ≤t·생존편향 시점별). **decile 풀**: `config.portfolio_pct` 설정 시 rank 절단 없이(top_n=후보수) 전 후보를 전략에 전달(전략이 decile 선택·분모=후보수 M1), None 이면 기존 top_n 절단.
- **benchmark**: `equal_weight_universe` 에 `liquidity_port` 필수 kw·동일 `tradable &= liquid`(엔진 대칭). **M2 trap 해소**: `turnover_total`/`cost_total` 0 강제 폐지 → `_turnover`+`costs.apply_cost_fraction` 로 멤버십 회전에 전략과 동일 `config.cost_bps` 부과(무비용 벤치=불공정 폐기). caveat 갱신(무비용→동일비용·small-cap 틸트).
- **DI 스레딩**(필수 kw 전파): `validation.walk_forward`·`s6_gate`(walk_forward_by_cost·sensitivity_analysis·run_s6_gate·_reproducible·G-3 bench·CLI)·`demo`·`profile`·`api/routes/backtest` 전부 `liquidity_port` 주입(CLI/route/demo/profile=`_select_liquidity_port`+finally close). signature/canonical decile 전환은 Phase 2-7.
- **TDD**: engine 3케이스(`_rank_at` 유동성 필터·decile 전 후보 풀·run end-to-end 선택변경)+bench 2케이스(유동성 멤버 제한·동일비용 turnover/cost>0). RED(unexpected kwarg)→GREEN. 기존 호출부 37곳 `liquidity_port=FakeLiquidityPort(None)`(pass-through=동작불변) 갱신 — bit-identical 불변성(invariance)·결과불변(profile) 봉인 유지. **전체 401 passed**·ruff/mypy clean.

### Phase 2-7 signature 동결 + canonical decile config + R2 측정게이트 (TDD·B1 trap·커밋 후속)

ADR-010 #3/#4/#6/#7 + R4 + B1 함정 해소 — 게이트가 **decile momentum** 을 검증하도록 정규값 동결하고 signature 3자(gate/route/ranking) 정합.
- **`compute_rule_signature` 동결필드 추가**: `portfolio_pct`·`decile_min_holdings`·유동성 3필드(min_price_floor·min_adv_dollar·adv_window_days). 전부 정규 동결 기본값 → route/ranking 인자 생략 시 매칭·비정규 게이트는 flip 불가. run_s6_gate rule_sig 도 config 신필드 전달.
- **`canonical_gate_config` decile 전환(B1 핵심)**: 기본값 = strategy `top_decile_equal_weight`·portfolio_pct 0.10·decile_min 20·lookback **252**·skip 21·기간 **2000-01-01~2026-06-18**(frozen). 정규 상수 모듈화(`_CANONICAL_*`·`_canonical_*()`) → canonical_gate_config·ranking_rule_signature 단일 출처(정규값 발산=영원히 false 함정 차단).
- **`ranking_rule_signature` R4 다리**: `top_n` 인자 제거(display 무관·decile 부분집합) → strategy/portfolio_pct/decile_min/cap/유동성/recovery/topN 정규값 + 요청 momentum(lookback/skip/group)만. 운영 Top5 요청도 검증 decile 과 lookback/skip/group 일치 시 flip. ranking route 호출 갱신.
- **CLI**: `canonical_gate_config()`(start=days[0] 주입 폐기·frozen 기간) + 전략 = config.portfolio_pct→`TopDecileEqualWeight`(None이면 EqualWeight 폴백).
- **R2 측정게이트(ADR-010 #7)**: `_R2_EXCESS_MAX=10.0`·`S6GateResult.r2_measurable`·evaluate_criteria 에서 `|worst_oos_excess|≤10` 판정 → `passed` 에 AND(G-3 가 전 fold>0 라 통과해도 2차 폭발 e7 은 R2 가 차단). write/print/notes 반영.
- **TDD**: s6_gate 6신규(signature 신필드 민감·canonical decile 동결·decile↔ranking signature 정합·lookback 불일치 mismatch·R2 폭발 fail·R2 정상 통과) + test_api flip 2케이스 top_n→lookback 축으로 교정. RED(unexpected kwarg/missing attr)→GREEN. **전체 pass**·ruff/mypy clean.
- ⚠️ 남은 Phase 2: ranking-API (c)사후 유동성필터(2-8)·sabotage 가드+decile 게이트 wiring smoke(2-9) → 빠른경로 sanity → 게이트 1회.

### Phase 2-8 ranking-API (c) 사후 유동성 필터 (TDD·커밋 후속)

ADR-010 #5(c) — ranking route 가 검증 decile 과 동일 PIT 유동성 유니버스로 display 일관.
- **`api/routes/ranking.py`**: momentum scores 산출 후 rank 전, `_select_liquidity_port`(backtest.adapters·frozen $5/$1M/20)로 `liquid_tickers(as_of=effective_as_of, candidates=set(scores))` 선필터 → scores 좁힘. **rules.rank_by_momentum 불변**(모듈경계 — api 가 data/backtest 조합·rules 안 건드림). cache.duckdb 부재면 Noop(필터 off·WARNING) → 기존 ranking 테스트 불변(cache 미빌드). 룩어헤드 ≤effective_as_of.
- **TDD**: cache(volume) 빌드 후 penny(close $4<$5) 제외·LIQ 유지. RED(PENNY 잔존)→GREEN. `_bar` volume 인자+low 0클램프(penny 안전). 전체 pass·ruff/mypy clean.
- ⚠️ ranking_rule_signature 정합은 2-7서 완료(route 호출 갱신). 남은 Phase 2: sabotage 가드+decile 게이트 wiring smoke(2-9).

### Phase 2-9 sabotage 대칭 가드 + decile 게이트 wiring smoke (TDD·커밋 후속)

8hr 게이트 전 마지막 배선 봉인 — engine↔bench 유동성 대칭 + decile 전체 경로 분 단위 검증.
- **sabotage 대칭 봉인**(`test_backtest_sabotage`): 동일 liquidity_port 가 engine(`_rank_at` 후보)·bench(멤버) 양쪽서 같은 종목 제외 — bench 유동성 제외 = 유니버스 축소 동치(total_return·equity_curve 일치). 비대칭 회귀(한쪽만 필터=G-3 인공 초과) 차단.
- **decile 게이트 wiring smoke**(`test_s6_gate`): decile config(portfolio_pct 0.1)+`TopDecileEqualWeight`+liquidity 로 `run_s6_gate` 가 끝까지 실행·fold>0 생성·rule_signature 가 config 신필드(decile/유동성) 반영 확인. canonical 경로와 **동일 코드**(축소 lookback 10·400일·30종목·2fold)라 B1 wiring 버그(빈 fold·signature 발산)를 분 단위로 차단. **실측: 통과**(decile 경로 end-to-end 무결).
- **TDD**: 2신규 케이스. 전체 pass·ruff/mypy clean.

## After (Phase 2 구현 완료 — 게이트 실행 대기)

ADR-010 방법론 전면교정 **구현 6단(2-4~2-9) 완료**. decile 전략·유동성 PIT 필터(engine/bench 대칭)·동일비용 벤치·signature 동결(B1 3자 정합)·R2 측정게이트·ranking (c)필터·sabotage 대칭·decile wiring smoke 전부 TDD green. **전체 테스트 pass·ruff/mypy clean**. 커밋: 3d1e7f6(2-4)·811ad46(2-5/6)·2f0bdc5(2-7)·7cea0d1(2-8)·(2-9).

**남은 작업(빠른경로 sanity → 게이트 1회)**: ① 빠른경로 sanity — 실데이터 축소 config(decile·2~3 fold)로 R2 excess 수렴·decay 정상화 확인(분 단위·`docker run` 격리). ② 통과 시 **신뢰구간 게이트 단 1회**(canonical decile·2000~2026·app/web 정지·`docker run --memory 20g`·MALLOC_ARENA_MAX=2·H3) → R2 판정 → G-1~G-8 → validated 결정. **⚠️ 게이트 실행은 사용자 격리 실행**(8hr·OOM·H2 cache+volume 재빌드 선행 필수).

### 빠른경로 sanity 실측 (2026-06-25·격리 컨테이너·실데이터)

- **🔴 H2 해소**: cache.duckdb(98.2M행) `volume` 컬럼 부재 확인 → `build_cache` 재빌드(컬럼 `[ticker,trade_date,close,adj_factor,volume]`·98,235,517행).
- **OOM 근본 규명**: 단일 `engine.run`(decile·2020~2026·78 리밸)이 리밸 **누적**으로 메모리 7GB+ 증가(재사용 DuckDB read 연결 버퍼 추정·memory_limit soft). 2 연결(price+liquidity)×기본 6GB 캡 = ~12GB(=구 "native peak 13GB" OOM 정체). `STOCKPICK_DUCKDB_MEMORY_LIMIT=2GB`+`MALLOC_ARENA_MAX=2`+격리(uvicorn baseline 제거)로 **단일 1패스 12g 내 완주**(oom=false). ⚠️ full walk_forward(12 백테스트 누적)는 12g 빠듯 — 게이트는 더 큰 메모리/MEM-fix 필요.
- **✅ 증폭기 제거 실증(핵심)**: 단일 1패스(walk_forward 없이 engine.run + equal_weight_universe) — 전략(decile) total_return=**−0.189**·벤치(동일비용 유동성 등가중)=**−0.101**·**EXCESS=−0.0878(\|excess\| 0.088 = O(1))**. 구 셋업 excess e7/e20 폭발 → 교정 후 O(1) 바운드. **0a 증폭기(top-5×cap19×무비용 full 벤치)가 root 였음을 실데이터로 재확인** — Phase 2 교정이 측정 척도 오염 제거(R2 임계 ≤10 한참 안쪽). [[finding-cap-compounding-explosion]] 해소 실증.
- **부수 신호(미확정)**: 이 구간 decile momentum 이 유동성 벤치를 −8.8%p 언더퍼폼(momentum 알파 약함 방향)·sharpe 0.0385 — 단 **공정 측정 가능 상태**. OOS/decay/per-fold R2 는 walk_forward 게이트(미실행)서 판정. 단일 1패스 ≠ validated.
- **다음**: 전체 walk_forward 게이트 1회(canonical decile·2000~2026) — 12g 메모리 마진 부족 → 더 큰 박스 격리 or MEM-fix(DuckDB 연결 주기적 재생성/malloc_trim) 선결. 사용자 격리 실행.

### MEM-fix — 연결 reset(누적 버퍼/단편화 해제) (TDD·debugging-discipline·커밋 후속)

게이트 OOM 근본 규명·수정(사용자 "MEM-fix 구현" 결정).
- **진단(실측·격리)**: ① diag1(momentum_scores 72회·reset 없음): RSS 1378→1636MB·malloc_trim 무효(−5MB)·**연결 close 시 1665→113MB 해제**. ② diag2(load_range 포함·12회마다 reconnect): reconnect 마다 1300→130MB 회복. → **hog = 재사용 DuckDB read 연결 버퍼풀**(DuckDB 내부 메모리·malloc_trim 무효·close 만 해제). full sanity(단일 1패스) peak ~7GB 중 일부는 python load_range Decimal 단편화(malloc_trim 회수). ③ walk_forward(12 백테스트 같은 연결): cap 1GB 로도 OOM(137·oom=true) — cap 만으론 부족(누적이 본질).
- **fix(adapters)**: `DuckDBPriceSeriesPort.reset()`·`DuckDBLiquidityPort.reset()`(close+reconnect·base_dir 저장)·`_malloc_trim()`(ctypes·비-glibc no-op)·`_reset_ports(price, liquidity)`(DuckDB 만 reset+trim·Fake/Parquet no-op·결과 불변). 같은 read-only cache 재연결이라 BLOCKING 결과 불변.
- **배선(최종)**: `validation.walk_forward`(각 IS·OOS run 후 reset)·`s6_gate.run_s6_gate`(G-3 bench loop 각 fold 후 reset) → **run 간** 누적 0(gate2 재실행서 11.9GB→4.4GB 급락 실측·reset 작동).
- **within-run reset 시도→폐기(실측)**: 단일 run 이 9-12GB climb(전 유동 유니버스·2020~2026 단일 1패스)해 between-run reset 만으론 게이트 OOM 잔존. run 내부 N 리밸마다 reset 시도 — ⓐ reconnect-within-run = **spike 유발 해로움**(더 일찍 OOM) ⓑ malloc_trim-only-within-run = **무효**(climb 은 DuckDB 내부 버퍼·glibc 아님·diag1 trim −5MB 일치). 둘 다 OOM → within-run reset **제거**(엔진/벤치 루프 원복). 경량 diag(momentum_scores+load_range 루프 1.5GB 바운드)와 실 engine.run 9GB 괴리 = rank_by_momentum decile 전 후보 물질화 등 미프로파일 요인(memray 후속).
- **TDD**: reset 결과불변 4케이스(price/liquidity reset 후 동일 쿼리 결과·Fake no-op·reset 후 쿼리 정상). 전체 pass·ruff/mypy clean.
- **🔴 결론(메모리 근본)**: 단일 decile 백테스트가 전 유동 유니버스서 **9-12GB peak = 12g cgroup 근본 초과**(비결정적 OOM). between-run reset 은 cross-run 누적만 차단 — full 게이트는 **≥20g 박스 필요**(이전 게이트도 20g·roadmap). 현 호스트 16GB 여유(타컨테이너 14GB 점유)로는 부족 → **신뢰성 게이트는 더 큰 메모리 환경서 사용자 실행**(코드는 reset MEM-fix 포함 준비 완료). 빠른경로 sanity(단일 1패스)는 증폭기 제거 이미 실증(EXCESS |0.088|).
