# 2026-06-23 S6-b 신뢰성 게이트 — momentum 룰 다년 백테스트 검증 → validated 판정

- **유형**: 플랜모드 승인
- **관련 기획/이슈**: stock-1st_plan §4.1(BLOCKING)·§4.4(검증)·M1 §6(신뢰성 게이트)·[[2026-06-22-백테스트-라이브실용화-DuckDB컬럼스토어]](선결)·[[2026-06-23-관측성-스택]](측정). 프로젝트 메모리 `project-backtest-perf-backlog`.
- **시작 시점 커밋**: `0a8c5e2` → **완료 커밋 범위**: `b66cd4f`(Task0)~`d99292c`(단락 봉인) + Task5 docs. 판정 `validated=false`(G-7 무결성 블로커).

## 의도/목적 — 왜 이 작업을 하나

원래 목표 = **S6-b 신뢰성 게이트**(`meta.validated=false→true`). 그동안은 선결(속도 실용화·관측성)뿐 — S6-b **본작업 미시작**(validated=false 불변). 본작업: 현재 **momentum 룰(프로토타입·단일팩터)**이 전구간(1962~2026·50,184종목·폐지 포함) 백테스트에서 **out-of-sample 강건한가**를 사전동결 기준 게이트로 판정 → validated 결정. ⚠️ **정직한 판정 도구**(통과 강요 아님·momentum fail 가능성↑면 false 유지가 정답). 임계 사전 동결(데이터로 임계 고르기=과적합 금지·M1 §6).

## 계획 (승인 플랜 백업)

**전문**: `.claude/plans/pro-3-5-twinkling-adleman.md`(승인본). 사용자 결정: 범위=**구축+풀 실행(~8hr)**·민감도=**비용 우선·완전성 후속**.

**기준표(사전 동결·모듈 상수·전부 AND)**: G-1 전 fold IS sharpe>0·G-2 전 fold decay_ratio≥0.5·G-3 OOS 등가중 벤치 초과>0·G-4 n_folds≥10·G-5 delisted≥30% AND n_delisted>0·G-6 비용 ±(5/10/15bps) 모두 G-2·G-7 bulk --verify PASS·G-8 fingerprint 재현. 하나라도 fail→validated=false 유지.

**재사용**: `validation.walk_forward(n_folds·purge)`·`decay_ratio`·`engine.run`·`benchmark.equal_weight_universe`·`adapters._select_*`·`MasterUniverse`·`storage.verify_parquet`·`GuardReport`. **신규** `backtest/s6_gate.py`(`sensitivity_analysis`·`S6GateResult`·`run_s6_gate`·`load_s6_gate_verdict`·CLI). **flip**: `s6_gate_result.json`(passed·rule_signature) → ranking/backtest meta.validated(signature 일치+passed만 true·보수).

**Task**: 0 백업·ADR-009 → 1 sensitivity(비용) → 2 run_s6_gate(G-1~8) → 3 flip 배선 → 4 CLI+verify+풀 실행(판정) → 5 문서. Task별 리뷰 2종·결과불변·룩어헤드·생존편향 가드 유지.

## Before — 수행 전 실측 (HEAD `0a8c5e2`)

- `meta.validated=false` 하드코딩(`api/routes/ranking.py:146`·`backtest.py:129`·flip 로직 없음). S6-b 게이트 미구현.
- 기존 골격: `validation.walk_forward(config,*,price_port,universe_port,identity,strategy,n_folds=3,purge_gap_days=None)→list[Fold]`(앵커드·완성)·`decay_ratio→GuardReport`(완성)·`GuardReport.sensitivity`=stub(빈 dict)·`engine.run`·`equal_weight_universe`(완성). `s6_gate.py` 없음·`run_s6_gate` 없음·`sensitivity_analysis` 없음.
- 데이터: 50,184종목(active 18,316·delisted 31,868=63.5%)·1962~2026·16,622 거래일·cache.duckdb 1억49만행. 분할≥10 충분. **bulk --verify 미실행**. zero-adjusted(adj_factor=0) 종목 다수(데이터 품질 리스크).
- 백테스트 1회 25.5분·peak 12GB → 10폴드 ~8hr(격리).

## After — 수행 후 실측 (Task별 누적·최종 Task5)

### Task4 풀 실행 1차 — verify_parquet OOM 발견·수정 (finding)
- **증상**: 격리 게이트(`docker compose run app python -m stockpick.backtest.s6_gate`) **exit 137(OOM)**·출력 로그 미플러시. s6_gate_result.json 미기록(시작시 무효화 후 OOM → `load_verdict` false·보수 정상).
- **근본 원인(이진 격리)**: verify **단독** 격리도 exit 137 → verify 가 OOM 주범(게이트 백테스트 아님). `storage.verify_parquet` 의 `duckdb.connect(":memory:")` 에 **memory_limit 미설정** → DuckDB 기본=호스트RAM 80%(31GB→~25GB) 타겟인데 컨테이너 cgroup 12g 무시 → 100M행 GROUP BY(중복검출) 스캔서 12g 초과 kill. `build_cache`(4GB)·`connect_readonly`(6GB)와 달리 verify 만 캡 없던 버그.
- **수정**: `_VERIFY_MEMORY_LIMIT`(env·기본 4GB) + `SET memory_limit` + `SET temp_directory=base_dir`(디스크 spill). 재실행 시 메모리 7.1GiB/12GiB 로 바운드(OOM 해소). 결과 불변(캡은 spill 일 뿐). 기존 verify 테스트 12건 통과.
- **연계**: 프로젝트 메모리 `project-backtest-perf-backlog` peak 항목과 동류(DuckDB cgroup 무인지). 단 이건 verify 경로 신규 발견.

### Task4 풀 실행 2차 — G-7 무결성 FAIL(verdict) + 백테스트 단계 OOM 발견·단락 (finding)
- **G-7 무결성 FAIL(verdict 확정)**: verify(캡 적용·정상 완료)가 `VerificationError` raise — **가격<=0 = 542,588행·OHLC위반 = 88,064행**(중복 0·adj_factor<=0 0·누락 0). 데이터 무결성 위반 → **validated=false 확정**. (예측한 데이터 품질 블로커 — 단 adj_factor=0 가 아니라 nonpositive price/OHLC. 데이터 정제는 범위 밖·후속). 부수: MasterUniverse degenerate 865종목 제외(정상).
- **백테스트 단계 OOM(operational finding)**: verify 통과(VerificationError catch→verify_passed=False) 후 walk_forward 백테스트 단계서 **exit 137**. 단일 풀 백테스트(11.9GB)가 3비용 walk_forward 누적·fold 보유·per-fold 벤치로 12g 경계 초과. → **알려진 제약**: 데이터 정제 후 게이트 재실행 시 mem_limit >12g(또는 백테스트 단계 메모리 최적화) 선결. 백로그 기록.
- **단락 설계(결정)**: G-7(데이터 무결성) 실패 = 데이터 신뢰 불가 → 손상 데이터 백테스트(G-1~G-6)는 **garbage-in·무의미**. `run_s6_gate` 가 `verify_passed=False` 면 백테스트 **미실행**하고 passed=false·g7=false 기록(전 G AND 라 G-7 하나로 확정). 정직(손상 데이터로 가짜 G 수치 안 냄)·OOM/8hr 동시 회피. `replace` note 보강·단위테스트 봉인.

### 종합 판정
- **validated = false** (G-7 무결성 블로커). momentum 룰의 OOS 강건성(G-1~G-6)은 **데이터 신뢰 불가로 미평가** — 게이트는 정직한 판정 도구이므로 손상 데이터로 통과/수치 산출 안 함. `s6_gate_result.json`(passed=false·G-7 fail·note) 기록. API `meta.validated` 는 flip 로직상 그대로 false(신호 일치+passed 아니면 false).

## 비교/회고

- **정직 판정 작동**: "게이트는 통과시키는 도구가 아니라 정직한 판정 도구"가 그대로 실현 — 데이터 무결성(G-7)이 막아 momentum 평가 자체가 부적절함을 정직히 노출. validated=false 유지가 정답.
- **풀 실행이 2개 실버그 발굴**: (1) verify_parquet DuckDB cgroup 무인지 OOM(수정), (2) 백테스트 단계 12g 경계 OOM(백로그). 8hr 추정이 무의미해짐(G-7 단락으로 ~4min 종결).
- **후속**: ① 데이터 정제(542K nonpositive price·88K OHLC 위반 원인 규명·EODHD 적재 경로) ② 정제 후 게이트 재실행 시 mem>12g ③ 그때 momentum G-1~G-6 실제 판정.

### 최종 검증 (다관점 적대적 워크플로우·4렌즈)
정직성·flip 정확성·문서 일관성·금융 BLOCKING 4렌즈 병렬 검증 + 종합. 결과:
- **HIGH 1건 발견·정정**: 0-price **근본원인 오귀속**. JUNP_old 표본 5행만 보고 "542K=_old/폐지 플레이스홀더"로 일반화했으나 실측상 **_old는 0.39%(1,965행)뿐·99.6%(504,591행)가 비-_old 일반티커**(VHAI·SWAV·HYPRQ·일부 epoch 날짜·원인 미상). verdict(validated=false) 불변이나 후속 정제 방향 오도(=_old 필터는 0.37%만 제거) → 6파일+메모리 정정·후속을 "비-_old 0-price 원인 규명"으로 재설정.
- **MEDIUM 2건(flip·forward-looking·현재 노출 0)**: ranking 기본 group=exchange vs canonical group=False 발산(게이트 PASS 후에도 기본 랭킹 false)·ranking↔equal_weight_top_n 동치 미봉인 → ADR-009 "flip 알려진 한계"에 기록(validated=true 전환 전 처리).
- **검증 누수(미검증→검증 오판) 0 확인**(flip 보수성 견고)·핵심 판정 정직·정확·일관 총평.
