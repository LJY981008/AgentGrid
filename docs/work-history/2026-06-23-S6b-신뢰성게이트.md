# 2026-06-23 S6-b 신뢰성 게이트 — momentum 룰 다년 백테스트 검증 → validated 판정

- **유형**: 플랜모드 승인
- **관련 기획/이슈**: stock-1st_plan §4.1(BLOCKING)·§4.4(검증)·M1 §6(신뢰성 게이트)·[[2026-06-22-백테스트-라이브실용화-DuckDB컬럼스토어]](선결)·[[2026-06-23-관측성-스택]](측정). 프로젝트 메모리 `project-backtest-perf-backlog`.
- **시작 시점 커밋**: `0a8c5e2` → **완료 커밋**: (완료 시·Task5)

## 의도/목적 — 왜 이 작업을 하나

원래 목표 = **S6-b 신뢰성 게이트**(`meta.validated=false→true`). 그동안은 선결(속도 실용화·관측성)뿐 — S6-b **본작업 미시작**(validated=false 불변). 본작업: 현재 **momentum 룰(프로토타입·단일팩터)**이 전구간(1962~2026·50,184종목·폐지 포함) 백테스트에서 **out-of-sample 강건한가**를 사전동결 기준 게이트로 판정 → validated 결정. ⚠️ **정직한 판정 도구**(통과 강요 아님·momentum fail 가능성↑면 false 유지가 정답). 임계 사전 동결(데이터로 임계 고르기=과적합 금지·M1 §6).

## 계획 (승인 플랜 백업)

**전문**: `.claude/plans/pro-3-5-twinkling-adleman.md`(승인본). 사용자 결정: 범위=**구축+풀 실행(~8hr)**·민감도=**비용 우선·완전성 후속**.

**기준표(사전 동결·모듈 상수·전부 AND)**: G-1 전 fold IS sharpe>0·G-2 전 fold decay_ratio≥0.5·G-3 OOS 등가중 벤치 초과>0·G-4 n_folds≥10·G-5 delisted≥30% AND n_delisted>0·G-6 비용 ±(5/10/15bps) 모두 G-2·G-7 bulk --verify PASS·G-8 fingerprint 재현. 하나라도 fail→validated=false 유지.

**재사용**: `validation.walk_forward(n_folds·purge)`·`decay_ratio`·`engine.run`·`benchmark.equal_weight_universe`·`adapters._select_*`·`MasterUniverse`·`storage.verify_parquet`·`GuardReport`. **신규** `backtest/s6_gate.py`(`sensitivity_analysis`·`S6GateResult`·`run_s6_gate`·`load_s6_gate_verdict`·CLI). **flip**: `s6_gate_result.json`(passed·rule_signature) → ranking/backtest meta.validated(signature 일치+passed만 true·보수).

**Task**: 0 백업·ADR-009 → 1 sensitivity(비용) → 2 run_s6_gate(G-1~8) → 3 flip 배선 → 4 CLI+verify+풀 실행(판정) → 5 문서. Task별 리뷰 2종·결과불변·룩어헤드·생존편향 가드 유지.

## Before — 수행 전 실측 (HEAD `0a8c5e2`)

- `meta.validated=false` 하드코딩(`api/routes/ranking.py:146`·`backtest.py:129`·flip 로직 없음). S6-b 게이트 미구현.
- 기존 골격: `validation.walk_forward(config,*,price_port,universe_port,identity,strategy,n_folds=3,purge_gap_days=None)→list[Fold]`(앵커드·완성)·`decay_ratio→GuardReport`(완성)·`GuardReport.sensitivity`=stub(빈 dict)·`engine.run`·`equal_weight_universe`(완성). `s6_gate.py` 없음·`run_s6_gate` 없음·`sensitivity_analysis` 없음.
- 데이터: 50,184종목(active 18,316·delisted 31,868=63.5%)·1962~2026·16,622 거래일·cache.duckdb 1억49만행. 분할≥10 충분. **bulk --verify 미실행**. zero-adjusted(adj_factor=0) 종목 다수(데이터 품질 리스크).
- 백테스트 1회 25.5분·peak 12GB → 10폴드 ~8hr(격리).

## After — 수행 후 실측 (Task별 누적·최종 Task5)

(Task0~5 완료 시 기입)

## 비교/회고

(완료 시 기입 — momentum 판정 결과·각 G·validated 결정)
