# 2026-06-29 다팩터 피벗 Sub-project A — 재무 데이터 PIT 파운데이션

- **유형**: 플랜모드 승인(브레인스토밍 → 다관점 워크플로우 9에이전트 + tech-researcher + 3 Explore + 2 Plan)
- **관련 기획/이슈**: [[plans/stock-1st_plan]] §4.1·§4.3 / 설계 스펙 [[../superpowers/specs/2026-06-29-다팩터-재무데이터-PIT파운데이션-design]] / 후속 of 공식 게이트 validated=false
- **시작 시점 커밋**: `be19f22` → **완료 커밋**: (단계별)
- **승인 플랜 백업**: `/home/code/.claude/plans/graceful-spinning-shannon.md`(아래 요약)

## 의도/목적 — 왜

공식 신뢰성 게이트(2000~2026·10fold) 완주 → **momentum 단일팩터 = 깨끗한 측정에서 진짜 무알파**(validated=false·R-2 PASS·oos_excess 전폴드 음수). plan §4.1 "깨끗한 측정 후 fail → 다팩터(momentum+재무 ROE/PB) 결합 피벗 정당" 도달.

**병목은 결합법이 아니라 재무 데이터**(실측 probe): 리밸시점 ROE 산출가능 종목 = 2만 중 **7~8개**(커버 0.03%). 이 작업이 끝나면 임의 시점 t·임의 종목의 ROE/P/B 를 **PIT(filed≤t)·올바른 엔티티·생존편향-안전**하게 얻어 결합 룰(Sub-project B)이 측정 가능해진다.

## 계획 (승인 플랜 요약 — 전문=플랜파일)

단계 **A1→A2→A3→A4**(의존순):
- **A1 폐지 ticker→cik 복구**: `data/cik_mapping.py` 신규 + `eodhd.fetch_id_mapping`(`/api/id-mapping`). `{ticker:(cik,delisted_date)}`. fallback=SEC cik-lookup-data.txt(명+폐지일). 🛑정지점1=50샘플 라이브 실측.
- **A2 PIT ticker_history + PitIdentityResolver**: `universe.py` 실 다행(현재사/폐지/재사용 비중첩)·`ticker_history.json` export·`identity.py PitIdentityResolver`(JSON 로드·`on<valid_to` 경계·다중매칭 raise=유일 방어). DI 3곳(demo:54·s6_gate:765·deps:63)·engine/api 0변경.
- **A3 companyfacts 백필**: `edgar.py` 증분+Checkpoint resume(bulk.py:52 재사용)·fy≥2009·`storage.write_financial_facts`(Parquet·daily_bar 동형·6필드+source+ingested_at)·duckdb_cache·`_financials.load` Parquet 전환(latest_as_of 봉인). 🛑정지점2=만 cik 격리 백필.
- **A4 커버리지 probe**: `backtest/coverage_probe.py`·fold별 재무율+결측 4분류+MNAR skew. 🛑정지점3=B go/no-go.

저장소: financial_facts=Parquet+DuckDB 캐시·ticker_history=PG 스키마 채우기+JSON export. 가드: 룩어헤드(filed≤t·on<valid_to·다중매칭 raise)·생존편향(멤버십=가격 단일출처·재무NULL=결측 not 배제)·엔티티 오조인(폐지일+명)·무결성(verify_financial_facts).

## Before — 수행 전 실측

- 재무 커버리지 probe(2026-06-29·읽기전용): as_of 2015=ROE 7종목/멤버 22,590(0.031%)·2020=8/23,703·2024=8/20,874. financials.json=9 distinct cik(파일럿·4571 fact).
- `cik_for(ticker, on=t)`: `identity.py:35` 시점 `on` 무시(noqa ARG002)·`universe.py:41 _SNAPSHOT_FLOOR=date(1900,1,1)` 단일 스냅샷.
- ticker_history PG 스키마 기존(migrations 0001·`valid_from/valid_to`·`db.py:205 upsert_ticker_history`)·현재 stock당 1행(1900~NULL).
- 재사용 가능: `edgar.fetch_companyfacts`·`_financials.latest_as_of`·`factors.financial_factors`·`bulk.py Checkpoint`·`storage` Parquet 패턴.
- 게이트 인프라(s6_gate·MasterUniverse·메모리 fix·:memory: cap) 완성·재사용.

## After — 수행 후 실측 (단계별 기입)

### A1 코드부 (eodhd.fetch_id_mapping + cik_mapping 모듈) — 완료
- `eodhd.py`: `fetch_id_mapping(symbol)` — `GET /id-mapping?filter[symbol]={SYM}.US&fmt=json` → 봉투 `data[0].cik` → 10자리 zero-pad(`zfill(10)`)·empty/null/비리스트=None. 공유 `_fetch_json`(토큰주입 1곳·`from None` 체인차단) 추출 → `_get_json_array`/`_get_json_object`(봉투) 분기. 토큰 누설 가드 유지.
- `cik_mapping.py` 신규: `resolve_delisted_ciks(fetch_cik, delisted)`(순수·미커버 카운트 로그·조용한 추측 0)·`store/load_delisted_ciks`(`edgar/delisted_cik.json`·ISO 날짜·라운드트립). 폐지일 동반(ticker 재사용 식별 키).
- 검증: `ruff check src tests` OK · `mypy`(strict) OK · `pytest tests/test_eodhd.py`(35) + `tests/test_cik_mapping.py`(5) 전 GREEN. 라이브 호출 0(httpx.MockTransport·fetch 주입).
### 🛑 정지점1 — 라이브 실측 + 판정(완료 2026-06-29)
- 라이브 200 폐지샘플 ID-Mapping 커버율(분모별): **전체 70.0%(140/200)** · 비구조적(F/Y/워런트/유닛/파산/_old 제외) 84.5%(82/97) · **in-scope(비구조적 ∧ US 주요거래소) 91.7%(55/60)**.
- 미커버 60 구조 분류: **45(75%)가 구조적 비-SEC신고**(외국ordinary F 24·ADR Y 7·워런트/유닛/권리 9·파산 Q 2·리네임 _old 3) → SEC XBRL 재무 없는 증권종류·**어떤 소스로도 복구 0**. in-scope 미커버 5(ESCO1·UST1[숫자접미]·KERA·LDO·RMV).
- **판정(사용자 비준)**: **ID-Mapping 단독 채택**. 동결 임계 80%의 분모(전체 폐지)는 외국주·워런트 등 ROE/PB 산출 불가 증권 포함 = 잘못된 프록시 → 실 목표(US 보통주 XBRL) 분모 91.7% 통과로 재해석. SEC fallback 미구축(실복구 ≤8pp·이름매칭 룩어헤드 위험).
- **A4 후속 의무**: 구조적 제외(외국/워런트/파산)는 A4 probe 결측 4분류 중 **'비신고사'**로 명시 카운트(조용한 생존편향 금지). in-scope 잔여 ~8%는 문서화된 한계.

### A2~A4 — 미착수

### 최종 (Sub-project A 완료 시 기입)
- 검증 결과:
- 변경 규모:
- 커밋:
