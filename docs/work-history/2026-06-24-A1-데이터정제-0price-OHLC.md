# 2026-06-24 A-1 데이터 정제 — EODHD 0-price·OHLC 위반 (S6-b G-7 블로커 해소)

- **유형**: 일반 구현(디버깅→정제)
- **관련**: [[2026-06-23-S6b-신뢰성게이트]](G-7 무결성 FAIL 블로커)·debugging-discipline. 후속 = A-2(백테스트 12g OOM)→게이트 재실행→momentum 실판정.
- **시작 커밋**: (직전 HEAD) → **완료 커밋**: (완료 시)

## 의도/목적

S6-b 게이트가 **G-7 무결성 FAIL**(validated=false 블로커)로 막힘. A-1 = 그 데이터 결함을 규명·정제해 verify(G-7) PASS → 게이트 재실행 가능케 함.

## 근본원인 (debugging-discipline 진단·실측 확정)

**2개 독립 EODHD 소스 데이터 결함**(우리 파싱 버그 아님 — 어댑터는 충실히 저장·라이브 재요청=영속 동일):
- **이슈① 0-OHLCV 봉** (close<=0 = 506,556행·전 OHLC<=0 = 542,588·4,010티커·8%): EODHD가 비거래일/폐지후를 0-OHLCV로 응답. 실봉이후 padding 49%+실봉구간내 산재 51%. `_row_to_bar`가 None만 drop·**0은 통과**(Decimal("0"))라 저장. 라이브 IBXG 재요청=2,029/6,128 동일(영속).
- **이슈② OHLC 순서 위반** (88,063행·close>0·①과 별개): EODHD가 저유동 종목에 **open=전일종가 carry-forward**(시가 호가 부재)·high=low=close=실거래가 → 가격 하락일 high<open. 라이브 GIX-UN=1,029/4,480 동일(영속·구조적). 분해: HLC일관·open만밖 81,774(clamp) + **HLC 자체 불일치(close>high 등) 6,289**(MPAC_old 등).

**분류**: 소스 데이터 결함(EODHD). 룩어헤드·생존편향·파싱버그 아님. 갭 = 저장 전 무결성 거부 안 함.

## 사용자 결정
- 이슈①: **drop**(close<=0·가격 없음·비거래일=원래 누락). 
- 이슈②: **open clamp**(close 실거래가 유효·보존). 잔존 close>high 6,289는 같은 원칙으로 통합.

## 통합 정규화 규칙 (close 불변·순서만 보정·없던 가격 invent 안 함)
```
close<=0 → drop(이슈①)
positives = [close] + ([high] if high>0) + ([low] if low>0)
high' = max(positives); low' = min(positives)   # close(실거래가) 포함하도록 확장
open' = close if open<=0 else clamp(open, low', high')   # carry-forward 아티팩트 제거
```
- 보장: 출력 전 OHLC>0 AND ordering 일관(verify nonpositive+OHLC 양 게이트 PASS). 정상 봉엔 **멱등**(불변). open carry-forward→실가·close>high→high 확장·close<low→low 확장·전부 일관.

## Before — 수행 전 실측
- Parquet 5.1G·578,330 파일(`exchange={EX}/year={YYYY}/{ticker}.parquet`)·100,149,294행. close<=0 506,556·OHLC위반 88,063. verify(G-7) FAIL.
- `_row_to_bar`(eodhd.py): OHLCV None만 drop·0 통과. `compute_adj_factor`가 close<=0 시 per-row WARNING(adj 산출불가·1 적용)이나 봉은 저장.

## After / 회고

(완료 시 기입 — 정제 행수·verify PASS·diff)
