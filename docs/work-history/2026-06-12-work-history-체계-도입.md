# 2026-06-12 work-history 체계 도입

- **유형**: 하네스/인프라
- **관련 기획/이슈**: 사용자 요구 — "구현 과정 히스토리를 남겨두자" (플랜 백업 + 의도/목적 + 전후 비교)
- **시작 시점 커밋**: `08262f8` → **완료 커밋**: (이 엔트리 동반 커밋)

## 의도/목적 — 왜 이 작업을 하나

구현의 "왜(의도)·어떻게(계획)·결과(전후 차이)"가 채팅 세션과 함께 휘발됨. 플랜모드 승인 플랜도 세션 밖에 남지 않음.
이 작업 후: 모든 구현이 `docs/work-history/` 에 의도→계획→Before→After→회고로 남고, 누락은 훅이 물리 차단.

## 계획

1. 템플릿(`docs/templates/work-history-template.md`) + `docs/work-history/INDEX.md` + HOME MOC 연결
2. `post-plan-approve.sh` — PostToolUse(ExitPlanMode) 리마인더 훅 (비차단, 플랜 전문 백업 유도)
3. `harness-drift-check.sh` 확장 — 대상이 `/` 종결이면 디렉토리 prefix 매칭, `backend/src/**`·`frontend/src/**` → `docs/work-history/` 매핑 2건 추가 (물리 강제)
4. 회귀 테스트 +3 케이스, CLAUDE.md 규약 편입(훅 9종), harness-update 스킬 §1·§2 갱신, 영구 메모리 등록

## Before — 수행 전 실측

- 구현 히스토리 체계 없음 — dev-log(일지)·ADR(결정)만 존재, 작업 단위 기록 부재
- 훅 8종 / drift 매핑 15개 (전부 정확 경로 매칭) / 회귀 테스트 33케이스
- 플랜모드 승인 후 플랜 보존 수단 없음

## After — 수행 후 실측

- 훅 9종 (+post-plan-approve) / drift 매핑 17개 (+src 2건, prefix 매칭 지원) / 회귀 테스트 36케이스
- 검증 결과: (커밋 직전 tests/run.sh + drift 통과 — 작업 리포트에 수치 기록)
- 전후 비교 자동화 수준: 의도/계획 = 작성 규약(훅 리마인드), 누락 방지 = drift 물리 차단, Before/After 수치 = 작성 시 실측 의무

## 비교/회고

- 의도 대비 달성도: 요구 3건(플랜 백업/의도 기록/전후 비교) 전부 체계화. "완전 자동 전후 비교"는 불가(의도·해석은 LLM/사람 작성 필요) — 자동화 가능한 부분(누락 차단·리마인드)만 훅으로, 나머지는 템플릿 강제 항목으로
- 계획과 달라진 것: 없음
- 후속: [ ] 첫 실제 구현(DB 스키마)에서 규약 실전 검증
