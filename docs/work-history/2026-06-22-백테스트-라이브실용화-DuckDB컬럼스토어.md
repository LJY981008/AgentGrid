# 2026-06-22 백테스트 라이브 실용화 — DuckDB 단일 컬럼 스토어 + momentum 부분 푸시다운

- **유형**: 플랜모드 승인
- **관련 기획/이슈**: [[2026-06-22-S6a-full_series-OOM수정]] 후속(OOM 회피했으나 속도 미달). S6-b 신뢰성 게이트(분할≥10=백테스트 10배)의 **선결**. [[decisions/ADR-007-백테스트-DuckDB-persistent-캐시]]·ADR-004(성능 재검토 트리거).
- **시작 시점 커밋**: `51e80e7` → **완료**: Task0 `fccab73`·Task1 `574418d`·Task2 `d7fda13`·Task3 `86942b2`·Task4 `6371433`·Task5 `2bdf1f4`·Task6 `3549f8d`·Task7(이 커밋). ⚠️ 풀 백테스트 ≥10× 미달=벤치 멤버십 load_range finding(후속 1순위)

## 의도/목적 — 왜 이 작업을 하나

S6-a 가 `full_series` 메모리 OOM 을 `load_range` 로 해소했으나 라이브 `/api/backtest`(50,184·5.1G·1억행) 가 **load_range 매 리밸 풀스캔 30초·21분+ 미완**(중단). 병목 = 메모리 아니라 **데이터 접근 구조**: Parquet ticker 가 파일명(`exchange=/year=/{ticker}.parquet`)이라 ticker 프루닝 불가 → 매번 window year 수만 파일 풀스캔.

라이브 백테스트가 돌아야 S6-b(룰 신뢰성 게이트)·`validated=true` 가능 → 이 속도가 선결. PoC 로 **DuckDB 단일 컬럼 스토어 + momentum 부분 푸시다운**(SQL 끝점 추출·Python Decimal 나눗셈) = 실용 속도 확정. ⚠️ `meta.validated=false` 불변(속도 실용화·검증 아님).

## 계획 (승인 플랜 백업)

**전문**: `.claude/plans/pro-3-5-twinkling-adleman.md`(승인본).

**핵심**: ① `data/duckdb_cache.py`(Parquet→`cache.duckdb` 단일 컬럼 table·원자 빌드·bulk --finalize 동반) ② momentum **부분 푸시다운**(`momentum_endpoints` SQL 2점 추출 + `factors.momentum_from_endpoints` Python Decimal) ③ `MomentumScorePort`(옵트인)·`DuckDBPriceSeriesPort`·`_select_price_port`(폴백 Parquet) ④ `engine._rank_at` isinstance 분기 ⑤ 결과불변 회귀(Task5 BLOCKING) ⑥ ADR-007. Task 0~7.

**PoC 검증(사용자 요구·실측 완료)**:
- 속도: Parquet 풀스캔 23초 → DuckDB 단일 table 7.6초(338만 반환) → **부분 푸시다운(18k 반환) 1.2초**.
- **결과불변**: 완전 SQL momentum 나눗셈 = DuckDB `DECIMAL/DECIMAL`→DOUBLE float 오차 **6/9 MISMATCH**(기각). **부분 푸시다운**(SQL `close*adj_factor` DECIMAL 곱·2점 추출·Python Decimal 나눗셈) = **6/6 bit-identical**(무한소수·adj_factor 소수 포함).

**critic REVISE 반영(2C 0·3M+Minor)**:
- MAJOR-1: `(ticker,trade_date)` 인덱스 EXPLAIN상 미사용(SEQ_SCAN) — 속도근원은 **단일 컬럼 table(578k glob 회피)**. 인덱스는 Task1 EXPLAIN 효용 확인 후만(미사용이면 제거). bit-identical 곱셈(DECIMAL(38,10)×(38,12)→(38,22)·float 승격 없음)·0/1-based 경계 = critic 실측 SOUND.
- MAJOR-2: 실데이터 **1962~2026·16,622 거래일·~770 월간 리밸**(360 아님)·벤치 load_range 2회/리밸 푸시다운 안 됨(테이블 가속만)·benchmark.py critical path 스코프. wall-clock = **Parquet baseline 대비 ≥10×**(bottom-up).
- MAJOR-3: `DuckDBPriceSeriesPort.load_range` ↔ Parquet 행집합 동치 단위 테스트(Task3).
- Minor/Missing: 중복봉 UNIQUE guard·원자 빌드(temp→os.replace)·부패 폴백·격리 실행·단일 Parquet 대안 ADR 기각.

## Before — 수행 전 실측 (HEAD `51e80e7`)

- 라이브 `/api/backtest`(50,184) = S6-a 후 OOM 회피·**load_range 30초/회·21분+ 미완**(중단).
- `engine._rank_at`(engine.py:152-) `load_range(tradable, window)`+`momentum_universe`(Python). 보유수익(engine.py:88)·벤치(benchmark.py:64,70) load_range. `_window_start`(S6-a).
- `rules/factors.momentum`(MomentumScore·end_idx=len-1-skip·start_idx=clamp·score=end/start-1·graceful·None)·`momentum_universe`→dict[ticker,MomentumScore].
- `_scan`(load_range_series·load_trading_days·`_iter_rows` 타입 narrowing)·`adapters`(ParquetPriceSeriesPort·_select_universe)·`ports`(PriceSeriesPort)·`bulk`(_apply_dates_and_snapshot·--finalize).
- Parquet 레이아웃 `daily_bar/exchange=/year=/{ticker}.parquet`(578,330 파일·ticker=파일명). duckdb>=1.5.3 의존 有. compose mem_limit 12g.
- PoC `cache.duckdb`(미커밋·정리됨): CREATE TABLE 36s·인덱스 11s·2.64GB·1억49만행.

## After — 수행 후 실측 (Task별 누적·최종 Task7)

**최적화 측정(쿼리 튜닝·사용자 요구)**: 인덱스 EXPLAIN = **SEQ_SCAN(미사용 확정·제거)**. momentum 쿼리 — 전체 ROW_NUMBER 3.4초·정렬 적재 3.2초(무용)·tot분리 6초 → **윈도우 [as_of-여유,as_of] + DESC 끝 2점 = 1.27초**(전체 대비 2.7×·Parquet baseline 23초 대비 **18×**·≥10× 달성). `momentum_endpoints` = 윈도우+DESC 2점 확정.

**Task1(duckdb_cache)**: `build_cache`(원자 temp→os.replace·멱등 전량재생성·중복(ticker,trade_date) ValueError guard·**인덱스 없음**[SEQ_SCAN 실측])·`connect_readonly`(read_only)·`cache_exists`. 게이트 PASS(ruff·format·mypy·pytest 3). 리뷰 2종: convention 3건 **전부 오탐**(S608 ruff 미발동·Path는 base_dir 객체연산[생성자0·pytest통과]·DECIMAL×DECIMAL→Decimal[code-reviewer 실측 반박]) + code-reviewer **APPROVE**(원자성·os.replace 동시성·Decimal 정밀도 실측 통과) → MEDIUM 2 반영(files 주석·빌드실패 stale docstring), LOW(중복guard/원자 테스트·memory_limit 라이브 실측) = 후속.

**Task2(momentum 부분 푸시다운)**: `data/duckdb_cache.momentum_endpoints`(SQL 윈도우 DESC 끝점 2점·`close*adj_factor` DECIMAL 곱·`_group_window_rows` 타입 narrowing) + `rules/factors.momentum_from_endpoints`(Python Decimal 나눗셈·`momentum()` 코어 복제·start.adjusted<=0 WARNING). 게이트 PASS(ruff·format·mypy·pytest 281). 신규 `tests/test_momentum_pushdown.py`(3 tests).

⚠️ **리뷰에서 결과불변 BLOCKING 버그 발견·수정(중간 리뷰 효과)**: 초안은 전체 `tot`(full-history count) 기준 graceful 판정 → code-reviewer 가 **2 CRITICAL**(① 윈도우 봉 0 종목 누락 ② sparse-window graceful 시 start 오발산) 실증 재현. 실측 검증 결과 **엔진 진짜 계약은 windowed momentum**(engine.py:148-152 "윈도우 봉 0=의도된 스테일 배제"·`load_range_series` setdefault 가 행 있는 종목만 dict 생성). 수정 = `tot` CTE·`_momentum_endpoints_fullscan` 제거, **윈도우 count `wn`** 기준 산출(`end_idx=wn-1-skip`·graceful `wn<=e2`)로 `momentum(windowed)`와 완전 동치. 재검증 **APPROVE**: 2 CRITICAL CLOSED, **10,800 조합(wn×skip×lookback) 0 불일치**·`wn<=e2 ⟺ start_idx==0` 수학적 항등 확인. 회귀 = GOLD(wn=0→양쪽 제외)·GPART(total=110·wn=19·e2=24=graceful-in-window·wn&lt;total 버그 케이스 실측 봉인).

**Task3(DuckDBPriceSeriesPort·MomentumScorePort)**: `ports.MomentumScorePort`(옵트인 Protocol·runtime_checkable) + `ports.momentum_window_days`(단일 출처 — engine._window_start·SQL 윈도우 동일 강제) + `adapters.DuckDBPriceSeriesPort`(cache.duckdb → load/load_range/full_series/trading_days/ticker_exchanges/momentum_scores·read_only 연결 1회 재사용·close). `_series_from_price_rows`(타입 narrowing·adjusted=close*adj_factor Python 곱=Parquet 동치). exchange 는 cache table 미적재(hive 파티션)→ticker_exchanges 만 Parquet 위임. engine._window_start 를 헬퍼로 리팩터(bit-identical). 신규 `tests/test_duckdb_port.py`(6 tests). 게이트 PASS(ruff·format·mypy·pytest 287). 리뷰 2종: convention **0건** + code-reviewer **APPROVE**(3 BLOCKING 불변식 컨테이너 실측 — load_range Parquet 동치 `as_tuple()`까지 동일·_window_start grid 무차이·SQL `ORDER BY ticker,trade_date` 동일) → MEDIUM(momentum_scores 테스트 graceful/None 파라미터)·LOW(full_series WARNING·WINDOW 리터럴→헬퍼) 반영. LOW(engine 미배선)=Task6.

**Task4(팩토리·bulk build_cache·배선)**: `adapters._select_price_port`(cache_exists→DuckDBPriceSeriesPort·부재 WARNING/부패 `except (duckdb.Error,OSError)`→ParquetPriceSeriesPort 폴백·기능 회귀 0) + `_close_price_port`(isinstance DuckDB→close·Parquet no-op). `bulk.main` commit **직후·close 전** `build_cache`(C1 일관·commit 성공 시 항상 재빌드로 close 실패에도 stale 회피·summary cache_rows). API route(backtest.py)·demo.py 를 `_select_price_port`+try/finally `_close_price_port` 로 배선(요청당 DuckDB 연결 누수 방지). 신규 테스트: test_duckdb_port(팩토리 present/absent/corrupt·close 4) + test_bulk(main finalize build_cache·PG-gated). 게이트 PASS(ruff·format·mypy·pytest 293). 리뷰 2종: convention **0건** + code-reviewer **APPROVE**(try/finally·result 스코프·corrupt 폴백 컨테이너 실측 — IOException⊂duckdb.Error 확인) → MEDIUM 반영(build_cache commit직후로 이동) + LOW(`_close_price_port` 명명 일관). M1(DuckDB+무스냅샷 시 PriceDerivedUniverse.full_series OOM 도달 가능·기존 위험·실배포는 finalize 가 스냅샷도 생성→MasterUniverse)=S6 full_series 후속.

**Task5(BLOCKING·결과불변 회귀)**: `test_backtest_invariance` 확장 — `_scenario`(폐지 A·평탄 B·갭 C·A,B=NASDAQ/C=NYSE)+`_make_cfg` 추출 후 `test_engine_duckdb_port_matches_parquet_and_fake`: build_cache→DuckDBPriceSeriesPort 백테스트가 Parquet·Fake 와 **equity_curve·전 지표(total_return·cagr·sharpe·sortino·max_dd·turnover·total_cost)·n_rebalances·n_delisted 전부 일치**(폐지청산≥1). **group_by_exchange False·True 양쪽**(ticker_to_exchange 가 그룹핑 키라 가장 민감). Task6 가드레일.

**Task6(engine momentum_scores 분기)**: `engine._rank_at` 에 `isinstance(price_port, MomentumScorePort)` 분기 — DuckDB=`momentum_scores`(SQL 푸시다운)·Fake/Parquet=`load_range+momentum_universe`. `ticker_to_exchange` 통합(`{k in scores and score is not None}`·rank_by_momentum 이 None 을 매핑 체크 전 skip 하므로 구 `{k in series}` 와 bit-identical). 게이트 PASS(pytest 295). 리뷰 2종(Task5 가드레일 동반): convention **0건** + code-reviewer **APPROVE**(5 불변식 독립 검증 — ticker_to_exchange 출력불변·scores 키 동일·윈도우 단일출처·룩어헤드·isinstance 분기, group_by_exchange True/False 컨테이너 실측 bit-identical) → LOW 2 반영(주석 정밀화·group_by_exchange=True 영구 seal 추가). 결과불변 회귀가 momentum_scores 경로로도 green.

**Task7(라이브 측정·문서)**: 실데이터(50,184·5.1G·1억49만행·1962~2026·16,622 거래일) 측정.
- **cache.duckdb 빌드**: 100,149,294행·50.3s·1.29GB(인덱스 없음).
- **단일 리밸 ≥10× + 실데이터 bit-identical(as_of=2026-06-18·tradable 18,316)**: Parquet `load_range+momentum_universe` **43.30s** → DuckDB `momentum_scores` **1.34s = 32.3×**(≥10× 목표 3배 초과). keys 18,311/18,311 동일·**score 불일치 0** — 실데이터 18,311종목 라이브 bit-identical 봉인.
- **풀 백테스트(774 리밸·monthly·top5)**: wall **4192s(~70분)·완주**(Parquet baseline 21분+ 미완 대비 완주). n_delisted=11·equity 775점·peak_mem **12,033MB**(12g 한계 직전·OOM 0). total_return 천문학적(=미검증 더티데이터[분할·zero-adjusted]·**validated=false 정당**·S6-b 별개).
- **병목 분해(단일 리밸)**: `momentum_scores` 1.27s/782MB vs **벤치 `equal_weight_universe` load_range(tradable 18k×window) 10.94s·3.65M PricePoint·+2GB** + 멤버십 필터 0.004s. → **푸시다운은 ranking 32× 달성**, 풀 wall-clock·peak 의 지배 비용은 **벤치 멤버십 load_range**(benchmark.py:64 — 멤버십 set 산출에 3.65M PricePoint 물질화·순수 낭비). 버그 아님(미최적화).

**판정**: ranking ≥10× ✅(32×)·실데이터 결과 bit-identical ✅·풀 백테스트 완주 ✅·OOM 0 ✅ / ⚠️ **풀 wall-clock ≥10× 미달**(70분·벤치 병목)·peak 12g 여유 없음. 플랜 Task7 예견 finding 확정.

## 비교/회고

momentum **부분 푸시다운**(SQL 끝점·Python Decimal)은 ranking 병목을 정확히 해소(43.3s→1.34s·32×)했고 실데이터 18,311종목에서 bit-identical 봉인. 그러나 "라이브 실용화"의 최종 목표(인터랙티브 풀 백테스트)는 **미달** — 병목이 ranking → **벤치 멤버십 load_range**(3.65M PricePoint/리밸)로 이동. 중간 리뷰 2종 protocol 이 Task2 결과불변 BLOCKING 버그(tot→wn)를 조기 차단한 게 최대 성과.

**후속(우선순위)**:
- [x] **벤치 멤버십 SQL 푸시다운** ✅ 완료(2026-06-23·아래 Task8).
- [ ] `connect_readonly` memory_limit(peak 12g 여유 확보·DuckDB 버퍼 캡).
- [ ] S6-b 신뢰성 게이트(분할≥10·폐지커버리지·민감도→validated=true)
- [ ] full_series Protocol 제거(+M1: DuckDB 무스냅샷 PriceDerivedUniverse.full_series OOM 경로)
- [ ] 증분 캐시/스케줄러(현 전량 재생성)
- [ ] zero-adjusted per-ticker WARNING 집계화(실데이터 다수·logging-rules 루프 spam — DEBUG/집계 1줄)

---

## Task8 (후속·2026-06-23): 벤치 멤버십 SQL 푸시다운

**의도**: Task7 finding 해소 — 벤치 `equal_weight_universe`(benchmark.py)가 매 리밸 `load_range(tradable 18k×window)`로 3.65M PricePoint 를 **멤버십 set 산출에만** 물질화(10.94s·+2.6GB/리밸·풀 백테스트 70분·peak 12g 의 지배 비용). 정작 쓰는 건 "구간에 데이터 있는 종목" 0.004s 판정뿐.

**대응**: `PriceSeriesPort.tickers_with_data(tickers,start,end)->set[str]` 신규 Protocol — SQL `SELECT DISTINCT ticker WHERE ticker=ANY AND trade_date BETWEEN AND close/adj_factor IS NOT NULL`(가격 미물질화). 4구현: `_scan.load_tickers_with_data`(Parquet)·`DuckDBPriceSeriesPort`(cache table)·`ParquetPriceSeriesPort`(위임)·`FakePriceSeriesPort`(메모리). benchmark.py `loaded=load_range`+`members=[tk if loaded.get(tk)]` → `members=tickers_with_data(...)`(set). 보유수익(load_range·`_holding_period_return`)·가중치는 미변경.

**결과불변(BLOCKING)**: 멤버십 = `load_range` 키집합 동치(동일 WHERE·봉≥1). `_iter_rows` 가 NULL close 행에 TypeError 라 effective 비포함 → DISTINCT 도 `close/adj_factor IS NOT NULL` 명시(self-contained·code-reviewer MEDIUM). 회귀: `test_tickers_with_data_equals_load_range_keys`(멤버십==load_range키·DuckDB/Parquet·경계 6케이스) + `test_backtest_invariance` 에 **벤치 equity DuckDB==Parquet==Fake** 추가(gbe 양모드). 게이트 PASS(ruff·format·mypy·pytest 293). 리뷰 2종: convention 0 BLOCKING + code-reviewer **APPROVE**(세 포트 동치·NULL 엣지·룩어헤드·set 변경 결과중립 컨테이너 실측) → MEDIUM(NULL 가드)·LOW(`set(members)`→`members`) 반영.

**측정(단일 리밸·as_of=2026-06-18·tradable 18,316)**: `tickers_with_data` **1.05s·peakΔ~90MB** vs 옛 `load_range+filter` **11.10s·2,680MB** = **10.5× 빠름·메모리 ~30× 절감**·멤버십 set 동치 True.

**풀 백테스트 재측정(774 리밸·전구간)**: wall **4192s(70분)→1532s(25.5분)=2.7×**·n_rebal/n_delisted/equity 동일(774/11/775·bit-identical). ⚠️ **peak_mem 12,033MB→11,740MB(거의 불변)** — 벤치 멤버십 2.6GB 물질화 제거로 wall 은 줄었으나 peak 유지. **peak 원천 미확인**(벤치 아님은 확정·단일 리밸 momentum_scores 는 782MB·cache 1.29GB 이라 버퍼풀 캐싱만으론 설명 안 됨 → 추측 금지·별도 instrument 조사 필요). peak 는 이 작업 이전부터 ~12g(pre-existing·OOM 0·12g 한도 내). → **후속: peak 원천 instrument 조사 + `connect_readonly` memory_limit 캡 효과 실측**(S6-b 다회 백테스트 OOM 여유).

**가드**: 룩어헤드(trade_date≤t)·생존편향(constituents 불변)·수정주가(가격계산 미변경)·`validated=false` 불변(속도 최적화).
