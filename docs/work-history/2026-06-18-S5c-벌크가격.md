# 2026-06-18 S5-c 벌크 가격 적재 (종목마스터 → Parquet 다년 EOD)

- **유형**: 플랜모드 승인
- **관련 기획/이슈**: S5(다년·전체유니버스) 4분해 3단계. S5-a(PG·G1)·S5-b(종목마스터 50,184) 후속. [[decisions/ADR-006-PG스키마-alembic-첫실사용]] 따름.
- **시작 시점 커밋**: `0f987cb` → **완료 커밋**: Task5(이 문서 동기화 커밋, S5-c 마지막)

## 의도/목적 — 왜 이 작업을 하나

S5-b 가 종목마스터(50,184 Common Stock·폐지 포함)를 PG 에 채웠다. S5-c 는 그 마스터 대상 **다년 EOD 가격을 Parquet 에 벌크 적재**(백테스트 1차 진실원본) + 가격에서 stock 날짜(listed_at/delisted_at) backfill. 생존편향-correct 다년 데이터셋 — S5-d 실 UniversePort·S6 게이트·백테스트 실검증의 입력. ⚠️ `meta.validated=false` 불변(데이터≠검증).

## 계획 (승인 플랜 백업)

**확정**: 전체 50,184(active 18,316+delisted 31,868)·가용 전부 history(~30yr)·**Parquet 벌크만**(PG daily_bar 동기 이연·백테스트=DuckDB/Parquet).

**critic 검증 반영**(self-review 2 + critic CRITICAL/MAJOR):
- self: ①날짜 backfill `delisted_at=max` 는 delisted 만(active 오표시 방지) ②expected=fetch 누적(마스터 전체면 미보유 0행 verify FAIL).
- C1: expected=**성공 fetch 누적**(소실 봉인)·마스터-vs-fetch 갭=**커버리지 요약**(verify FAIL 아님). master/fetched/empty/failed/coverage% 보고.
- M1: stock UNIQUE(ticker) 없음 → update_stock_dates ticker 유일 가정·시작 시 assert.
- M2: delisted_at_source='eodhd_last_bar_estimate'(추정 폐지일 마킹).
- M3: write 는 (ticker,year) 파일단위 atomic(per-ticker 아님)·체크포인트가 유일 재개 진실·tmp 정리.
- M4: transient(5xx/timeout) 재시도(429만 아니라)·일일쿼터 graceful stop.
- G8: verify 끝에 1회만(per-ticker O(n²) 회피·per-batch 거절).

5 Task: ①work-history ②db master_securities/update_stock_dates + storage load_trade_date_bounds ③bulk.py 체크포인트·재시도 ④bulk.py run_bulk(verify 1회·커버리지·날짜 backfill) ⑤라이브 스모크·문서. (전문: `.claude/plans/pro-3-5-twinkling-adleman.md`)

## Before — 수행 전 실측

- `stock` 50,184 security 적재됨(S5-b·active 17,546+delisted 31,755 라이브). `daily_bar` Parquet = 데모 9종목 + 잔여 18파일만. PG daily_bar 미동기.
- `data/ingest.py:ingest_tickers` = 순차·**매 종목 verify_parquet(O(n²))**·재시도/체크포인트 없음(데모용).
- `eodhd.fetch_daily_bars(ticker, start, end)`·`EodhdRateLimitError(retry_after_seconds)`·`_get_json_array` transient→`EodhdResponseError ... from None`.
- `storage.write_daily_bars`(G1 read-merge-write·(ticker,year) atomic)·`verify_parquet(expected=)`·`build_expected`·`list_dataset_tickers`. `db.connect`·`master_tickers`·`sync_daily_bars_from_parquet`·`upsert_stocks`(listing_status·(cik,ticker) UNIQUE). stock 에 delisted_at_source 컬럼 존재(0001·미채움).
- alembic head=0003. 테스트 246 passed.

## After — 수행 후 실측

5 Task = 5커밋(58a73ce Task1·7d74222 Task2·d415ae5 Task3·c632b6c Task4·이 커밋 Task5). `0f987cb..HEAD` = **8파일 +646/-2**.

**검증**: ruff·ruff format·mypy(strict)·pytest 전부 통과. 테스트 **246→259(+13)**: db +4(master_securities enum·update_stock_dates active/delisted 분기·trade_date_bounds)·storage(load_trade_date_bounds 2)·bulk +9(체크포인트 라운드트립/우선·재시도 429/5xx/4xx/auth/429소진·run_bulk active/empty/delisted+날짜backfill+재개skip+verify·--limit). alembic 0003 불변.

**라이브 스모크(`--limit 20`)**: `master=50,184·fetched=20·empty=0·failed=0·verify_passed=1`. 20종목(A·AA·AAAB…) 다년 EOD→Parquet 적재. 날짜 backfill 실측:
- active(A=Agilent·AA=Alcoa): `listed_at` 만(A=1999-11-18)·**delisted_at NULL**(active 오표시 0).
- delisted(AAAB·AAAGY·AAAID): `listed_at`+`delisted_at`(max=추정 폐지일)+`delisted_at_source='eodhd_last_bar_estimate'`.
- orphan 9(데모 AAPL 등 expected 밖) = **경고만**(verify FAIL 아님·설계대로). 체크포인트 20 done.
- ⚠️ **전체 50,184 풀런 = 운영자 트리거**(`docker compose exec app python -m stockpick.data.bulk`·수시간·재개 가능). 스모크로 파이프라인 입증 완료.

**구현**: `data/bulk.py`(Checkpoint JSONL·fetch_with_retry·run_bulk·main --limit)·`db.master_securities`/`update_stock_dates`·`storage.load_trade_date_bounds`.

## 비교/회고

- **의도 대비 달성도 100%(파이프라인)**: 종목마스터 대상 다년 EOD 벌크 파이프라인(체크포인트 재개·재시도·verify 1회·날짜 backfill·커버리지)을 스모크로 입증. 전체 적재는 운영자 단발 실행(데이터 deliverable). `meta.validated=false` 불변.
- **계획과 달라진 것**:
  - self-review 2(날짜 backfill delisted 한정·expected=fetch 누적) + critic 1C+4M(C1 expected 통일+커버리지·M1 ticker유일 assert·M2 delisted_at_source·M3 체크포인트 유일진실·M4 transient+쿼터) 전부 선반영 — 구현 중 막힘 0.
  - 라이브 스모크가 설계대로 동작(active/delisted 날짜 분기·orphan 경고). 라이브 버그 0(S5-b 의 다중클래스 같은 surprise 없음 — 스키마/upsert 가 이미 안정).
- **후속**: [ ] 전체 50,184 풀런(운영자·수시간) · [ ] S5-d 실 UniversePort(MasterUniverse·G7)·ticker_history EXCLUDE·시점 cik·거래소 정밀화·S6 게이트→validated=true · [ ] PG daily_bar 동기(온디맨드)
