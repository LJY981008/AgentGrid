# 2026-07-02 — 랭킹 산출 OOM 해소(cache 푸시다운 + 타깃 로더)

## 의도 / 목적
`GET /api/ranking`·`POST /api/rounds`(compute_ranking) 가 실데이터에서 **12g OOM(exit 137)**.
근본 = 전 트리 full-Parquet 로드 4곳. debugging-discipline 로 단계별 RSS 계측 후 타깃화.

## Before — 근본 지점 실측(격리 단계별 ru_maxrss)
- `load_adjusted_series`(전 트리 ≤as_of 수정주가 풀로드) → momentum_universe: 50k×26년 메모리.
- `ticker_exchanges`=load_ticker_exchanges(Parquet GROUP BY 전 트리): **4GB·36s**.
- `load_financial_facts`(2.68M 전량): **5.3GB**.
- `load_close_as_of`(전 50k arg_max): **7.2GB**. + routes/tracking create_round 이 **또** 전량 호출.
누적으로 12g 초과 OOM.

## After — 수행
1. **momentum**: `load_adjusted_series` 전량 → **유동성 필터를 momentum 앞**으로 이동 + `_select_price_port`(cache.duckdb `MomentumScorePort` 끝점 푸시다운·ADR-007)·부재 시 windowed `load_range`. 유동 후보는 최근 데이터 보유 → windowed=전-시계열 momentum(bit-identical·엔진 `_rank_at` 동일 경로).
2. **ticker_exchanges**: `stock_snapshot.json`(PG파생·50184종목·수MB·즉시)에서 exchange 맵 — Parquet 4GB 스캔 폐기(부재 시 폴백).
3. **load_close_as_of**: `tickers` 필터 추가(랭킹 enrich·라운드 스냅샷은 top20+이월 티커만·전 50k 스캔 회피).
4. **financial facts**: `storage.load_financial_facts_for(ciks)`·`_financials.load_financial_facts_for`(per-cik `<CIK>.parquet` 직독·enrich 는 top20 cik 만) — 2.68M 전량 회피.

## 검증(컨테이너·실데이터)
- 단계별 계측: momentum pushdown 4012종목·40s·4GB(전량 대비 격감).
- `POST /api/rounds` HTTP 200·**oom=false**(라운드 id 생성·top20=20). 이전 exit 137 → 해소.
- `GET /api/ranking` 87s→**27s**(4GB 스캔 제거). 잔여 27s=유동성 ADV 계산(별개 백로그).
- ruff/mypy(strict) OK · 전체 pytest EXIT=0 · 기존 test_api 랭킹 회귀 green(행위 보존 — 합성 데이터는 window 가 전 구간 덮어 동일).

## 잔여 백로그
- 유동성 필터(query_liquid_tickers) 50k 후보 ADV 27s — 대시보드 UX 개선 여지(후보 사전 축소/캐시).
