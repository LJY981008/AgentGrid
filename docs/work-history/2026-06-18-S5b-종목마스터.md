# 2026-06-18 S5-b 종목마스터 채움 (EODHD 유니버스 → PG stock/ticker_history)

- **유형**: 플랜모드 승인
- **관련 기획/이슈**: S5(다년·전체유니버스 적재) 4분해 2단계(S5-a 적재안전성 완료 후). [[decisions/ADR-006-PG스키마-alembic-첫실사용]] 따름·[[2026-06-18-S5a-적재안전성]] 후속. EODHD 결제 후.
- **시작 시점 커밋**: `bb042d5` → **완료 커밋**: (완료 시 기입)

## 의도/목적 — 왜 이 작업을 하나

S5-a 가 PG 코어 스키마(stock·ticker_history·daily_bar)·적재 안전성을 깔았다. S5-b 는 **종목마스터를 실제로 채운다** — EODHD 유니버스(Common Stock·폐지 포함)를 라이브로 받아 `stock` 적재. 생존편향-correct 의 물리적 근거("무엇이 존재하는가")이자 S5-c 벌크 가격 적재의 대상 목록·expected 원천(G2). 끝나면: 폐지 포함 보통주 마스터 + 현재 ticker_history 스냅샷 + cik enrich + G2 헬퍼. `meta.validated=false` 불변.

## 계획 (승인 플랜 백업)

**확정 결정**: ①유니버스=Common Stock 필터(symbol-list Type) ②날짜·active/delisted 상태 → S5-b 는 존재+listing_status 플래그만, 날짜(listed_at/delisted_at)는 S5-c 가 가격 min/max trade_date 로 backfill.

**critic 2회 적대적 리뷰 반영**(BLOCKING/MAJOR):
- C1: 서버 `type=common_stock` + **클라 Type 필터 1차(항상 정확·드롭 WARNING)**·서버는 페이로드 최적화(probe 무관 rework 0). "확인됨" 거짓 제거→Task6 실측.
- C2: ticker_history EXCLUDE 제약 = **S5-d 재이연**(무한스냅샷 충돌 회피)·0001 docstring 주석 정정.
- B1: active-wins(ON CONFLICT(cik))는 **resolved cik 전용**·unresolved(NULL) overlap=**2행 의도 공존 dedup 금지**(생존편향). 두 모집단 disjoint.
- M-a: ticker_history 스냅샷 **stock.id 당 1행**(ticker 당 아님·2행 ticker 면 2 스냅샷).
- M-b: Task6 M1 쿼리 `cik IS NULL` 스코프 + resolved-dup 0 체크.
- M2: 전 종목 OTC → group_by_exchange 무의미(group=all 권장)·S5-d 거래소 정밀화. M3: 빈 마스터·demo 9종목 OTC vs daily_bar NASDAQ 불일치.

6 Task: ①work-history ②migration 0002(listing_status)+0001 주석 ③eodhd Common Stock 필터·active/delisted 분리 ④db upsert_stocks status·master_tickers ⑤universe.py 오케스트레이션 ⑥라이브+문서. (전문: `.claude/plans/pro-3-5-twinkling-adleman.md`)

## Before — 수행 전 실측

- `stock` 테이블 **빈 상태**(S5-a 0001 마이그레이션이 스키마만 생성·`upsert_stocks` 프로덕션 호출자 0). demo 9종목은 Parquet daily_bar 만(exchange=NASDAQ/NYSE), PG stock 엔 없음.
- `eodhd.iter_universe(include_delisted=True)` = 활성+폐지 2콜 병합·Stock(cik=""·exchange=_map_exchange("US")→OTC·listed_at/delisted_at=None)·Type 미캡처·active/delisted 미태깅.
- `db.upsert_stocks(conn, stocks, *, source, ingested_at)`(S5-a)·`upsert_ticker_history`·`connect`. listing_status 컬럼 없음(0001).
- symbol-list 명세: `type` 쿼리파라미터(common_stock 등)·응답 `Type`("Common Stock") 확인(`docs/apis/eodhd/exchanges-api-list-of-tickers-and-trading-hours.json`).
- 테스트 238 passed. alembic head=0001. EDGAR ticker_cik 10,414건·EODHD_API_KEY 적재됨.

## After — 수행 후 실측 (완료 시 기입)

> **진행중** — Task 1~6 완료 시 채움(검증·라이브 유니버스 실측·diff·SHA·회고).

## 비교/회고 (완료 시 기입)

> **진행중**

- 의도 대비 달성도:
- 계획과 달라진 것 + 이유:
- 후속: [ ] S5-c 벌크 가격+날짜 backfill+expected shortfall wiring · [ ] S5-d 실 UniversePort·ticker_history EXCLUDE·시점해소·거래소 정밀화
