# 2026-06-18 S5-a 적재 안전성 선결 (PG 스키마·G1·db.py)

- **유형**: 플랜모드 승인
- **관련 기획/이슈**: S5(다년·전체유니버스 적재) 4분해 중 첫 단계. [[plans/M1-데이터파이프라인]] §4 S5·§7 D2/D3/D5 · [[decisions/ADR-006-PG스키마-alembic-첫실사용]] · 설계 스펙 `docs/superpowers/specs/2026-06-18-S5a-적재안전성-설계.md`
- **시작 시점 커밋**: `ffd4636` → **완료 커밋**: Task7(이 문서 동기화 커밋, S5-a 마지막)

## 의도/목적 — 왜 이 작업을 하나

EODHD EOD Historical($19.99) 결제 완료 → S5(전체 미국 유니버스·폐지 포함·다년 적재) 잠금 해제. 단 벌크 전 P0 BLOCKING 갭(데이터 소실·생존편향 누수)을 안 고치면 돈 걸린 데이터를 오염시킨다. S5 를 4분해(a 적재안전성 → b 종목마스터채움 → c 벌크오케스트레이션 → d 실UniversePort+S6게이트)하고, 첫 단계 **S5-a = 적재 안전성 선결**을 구현한다. 끝나면: 벌크(S5-c)가 안전하게 돌 토대 + 종목마스터(S5-b)가 채울 PG 스키마·repo 함수 확보. ⚠️ `meta.validated=false` 불변(검증은 S5-d 후 — 결제·스키마만으론 검증 아님).

## 계획 (승인 플랜 백업)

S5-a 범위 3가지: ① PG 스키마(alembic 첫 실사용 — stock+ticker_history+daily_bar 빈 테이블 생성) ② G1 Parquet write read-merge-write(다년 증분 소실 봉인) ③ data/db.py(PG repo·Parquet→PG 단방향 동기 함수, 합성 멱등 테스트). G6(httpx 토큰 가드)는 이미 구현됨 → 검증+docstring만.

확정 결정(사용자 승인): 4분해·종목마스터 PG/alembic·첫 마이그레이션 3테이블 전부·**R1 stock surrogate BIGINT PK + cik UNIQUE**.

critic 적대적 리뷰 반영(BLOCKING):
- **C1 cik `""`≡NULL**: 코드 전반 `cik=""`(NULL 아님) → 부분 UNIQUE 충돌 → repo 경계에서 `""`→SQL NULL 매핑(types.Stock 불변).
- **C2 source/ingested_at**: types.Stock 에 없음 → `upsert_stocks(*, source, ingested_at)` 파라미터.
- **C3 G6 기구현**: `data/__init__.py:configure_logging` 가 httpx/httpcore WARNING 이미 설정·`api/app.py:28` 호출 → 새 구현 불요.
- M1 버전핀 실측 · M2 INSERT ON CONFLICT(COPY 아님) · M3 dedup 신규우선+value-replacement 테스트 · M4 CI postgres.

7 Task: ①work-history+ADR-006 ②alembic 스캐폴드+compose 마운트 ③첫 마이그레이션 ④G1 read-merge-write ⑤G6 검증+docstring ⑥data/db.py+CI postgres ⑦문서동기화+게이트. (전문: `.claude/plans/pro-3-5-twinkling-adleman.md`)

## Before — 수행 전 실측

- PG 미사용(스키마 0). `compose.yaml` postgres 서비스만 떠 있고 테이블 없음. alembic 미설치(`pyproject.toml` deps = duckdb·fastapi·httpx·pyarrow·uvicorn).
- `data/storage.py:write_daily_bars`(241-306) = `(ticker,year)` 통파일 **덮어쓰기**(read-merge 아님 — docstring 261-267 에 소실 위험 경고만).
- `data/__init__.py:configure_logging`(18-29) = httpx·httpcore WARNING **이미 설정**(G6 기구현). `tests/test_data_init.py` 통과.
- `types.Stock`(types.py:40-54) = cik·ticker·name·exchange·listed_at·delisted_at (source·ingested_at 없음). cik 는 `str`(`""` 폴백, NULL 아님 — eodhd.py:243·edgar.py).
- 테스트 231 passed. DATABASE_URL 이미 compose 제공(`postgresql://stockpick:stockpick-local@postgres:5432/stockpick`).

## After — 수행 후 실측

7 Task 완료. 6커밋(0fe92a8 Task1 docs·1e33649 Task2·9b243f9 Task3·a8dc904 Task4·af42f71 Task5·bd0bdd3 Task6), `ffd4636..HEAD` = **18파일 +972/-19**.

**검증(전부 실측)**:
- 게이트: `ruff check`·`ruff format --check`·`mypy(strict)`·`pytest` 전부 통과. 테스트 **231→237(+6)**: storage +2(연도분할 부분적재 소실 0·overlap 신규값 우선)·db +4(미해소 cik 2개 적재[C1]·cik 충돌 갱신·동기 멱등·고아 탐지).
- alembic: `upgrade head → downgrade base → upgrade head` 재현 OK. 3 코어 테이블 + 33 파티션(daily_bar 1995-2026+DEFAULT) 생성. CHECK(음수가·OHLC·adj<=0) INSERT 거부 실측.
- 훅 회귀: PASS=38 FAIL=0.
- PG repo 테스트는 compose postgres 대상 실행(skip 아님 — DATABASE_URL 연결). CI 에 postgres:18 서비스 추가.

**구현 결과**:
- `migrations/`(alembic env.py·0001_create_core_tables) + `alembic.ini` + compose 마운트. 의존성 alembic 1.18.4·psycopg 3.3.4·SQLAlchemy 2.0.51(uv.lock 실측 고정).
- `data/storage.write_daily_bars` read-merge-write 전환(소실 봉인·atomic os.replace·신규우선 dedup). read-back 파티션컬럼·nullable 보정(select+cast).
- `data/db.py` 신규: connect·upsert_stocks(cik""→NULL)·upsert_ticker_history·sync_daily_bars_from_parquet(epoch_us 로 pytz 회피)·find_orphan_tickers.
- G6 기구현 확인(신규 코드 0). 문서 동기화(CLAUDE.md·python-conventions·PLAN_STATUS·RESUME·HOME·ADR-006).

## 비교/회고

- **의도 대비 달성도 100%**: 벌크 전 적재 안전성(PG 스키마·소실 봉인·동기 함수) 확보. S5-c 벌크가 안전하게 돌 토대 + S5-b 가 채울 스키마 완성. `meta.validated=false` 불변.
- **계획과 달라진 것**:
  - critic 적대적 리뷰가 3 BLOCKING 선반영(C1 cik""≡NULL·C2 source/ingested_at 파라미터·C3 G6 기구현) — 구현 중 막힘 없이 통과. C3 덕에 Task5 가 구현→검증만으로 축소.
  - 구현 중 발견(계획에 없던 실측 함정): ① Parquet read-back 이 Hive 파티션 컬럼(exchange/year) 추가 + nullable 화 → `select(컬럼)+cast(schema)` 필요 ② DuckDB TIMESTAMPTZ→Python 변환이 pytz 의존(미설치) → `epoch_us` bigint 추출 후 Python UTC 재구성으로 회피(새 dep 0·원본 시각 보존).
- **후속**: [ ] S5-b 종목마스터 채움(유니버스 실측·UPSERT·ticker_history EXCLUDE 구간중첩 제약) · [ ] S5-c 벌크 오케스트레이션(진입점 configure_logging 호출·G4/G5/G8) · [ ] S5-d 실 UniversePort+S6→validated=true
