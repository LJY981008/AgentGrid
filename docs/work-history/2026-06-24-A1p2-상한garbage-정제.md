# 2026-06-24 A1p2 상한 garbage 정제 — EODHD $1M sentinel·거대 adj_factor·수익률 캡 (G-3 폭발 해소)

- **유형**: 일반 구현(디버깅→정제). [[2026-06-24-A1-데이터정제-0price-OHLC]](하한 0-price·이번은 상한) 후속.
- **시작 커밋**: (직전 HEAD) → **완료 커밋**: (완료 시)

## 의도/목적

S6-b 게이트 **첫 완주**(exit0·10폴드·G-7 PASS·validated=false). 단 **G-3(OOS 등가중 벤치초과)=oos_excess −10^80 = 수치 폭발 버그**(momentum 판정 아님). 근원 = **EODHD 상한 garbage**가 등가중 벤치 복리를 발산. A1p2 = 정제해 깨끗한 momentum 판정 가능케 함.

## 근원 (debugging-discipline + 다중전문가 워크플로우 2회·쿼리 실측 검증)

EODHD가 **상장이전/결측 구간을 $999,999.9999 max-value sentinel로 back-pad**(LUV·MRVL 같은 legit 우량주 초기 봉에도 침투·O=H=L=C 일관). `compute_adj_factor`는 raw<=0·adjusted<=0·None만 방어 → sentinel(양수·일관) 전 게이트 통과. 등가중 벤치(`equity*=1+pret`)서 sentinel bar가 한 기간 +200,000x → 복리 e80 발산.
- 우리 코드 sentinel 생성 0(grep)·라이브 재요청 동일(GHGH close·ATDS adjusted_close=$1M÷페니→adj_factor 1e10). EODHD 영속 소스결함(A-1 하한과 동류·상한).

## 확정 정밀 spec (검증 워크플로우·쿼리 실측·9충돌 검증)

**전부 per-bar**(per-ticker drop 금지 — LUV/MRVL legit 삭제=생존편향 BLOCKING). env-tunable(검증 동결 기본값):
- **L1** `close ∈ [999999,1000001]` drop (165,447행·raw close sentinel·`STOCKPICK_SENTINEL_PRICE`)
- **L2** `round(close×adj_factor,2)=1,000,000` drop (732,989행·adjusted $1M pin)
- **L3** `adj_factor ≥ 100,000` drop (1,086,909행·ATDS uniform·legit 역분할 천장 29,081의 3.4배 위·**≥10,000은 XSPA legit 오제거라 기각**)
- **L4** `ret = clamp(exit/entry−1, −0.95, +19.0)` (engine `_holding_period_return`·**필수 1차 방어선**: L1~L3 후에도 tiny분모[entry $0.0001·5.7M행<$0.01]×forward sentinel로 e9 잔존. K=+19.0=GME 2021 16.2x 물리천장 보존·`period_return_cap` config 필드→fingerprint). BRK-A($809,350) 세 술어 0행(legit 무손실).
- **verify(G-7) 확장**: L1·L2·L3 count=0 게이트(정제를 사전동결 무결성 기준으로 박아 과적합 의심 차단).

## BLOCKING 리스크 (검증됨·완화)
- R1 생존편향: per-bar만. R4 과적합: 임계·K 사전동결·verify 게이트 박음·env override는 fingerprint(L4)/verify(L1~L3)로 추적. R3 결과영향(BLOCKING): caveat+fingerprint+캡 on/off 차이 정량화. R2 룩어헤드: 캡 무관·단 per-bar drop 갭의 `_rank_at` 스테일 배제 회귀테스트 필수.
- 벤치 시총가중 대안 = 불가(value 전량 NULL·shares 없음·volume 역상관) → 등가중 유지.

## Before / After

### 구현 정정 (리뷰 반영 — spec L4 동결값 변경)

| 항목 | spec(동결 전) | 구현(리뷰 후) | 근거 |
|---|---|---|---|
| L4 하한 floor | `clamp(ret, −0.95, +19.0)` | **floor 제거·상한 cap만** `min(ret, +19.0)` | `ret=exit/entry−1`, entry>0(가드)·exit≥0 → **수학적으로 ret≥−1**. 폭발은 전부 상한(극소 진입가·sentinel exit). floor −0.95 는 폭발 방어 0이고 정상 손실 [−1,−0.95)만 마스킹=**낙관편향**. 컨벤션+로직 리뷰어 독립 지적 일치 → 정직하게 제거 |
| cap 동결 우회 | fingerprint 만 | **fingerprint + compute_rule_signature** | env override(STOCKPICK_RETURN_CAP)로 비정규 cap 게이트 통과해도 signature 불일치 → validated flip 차단(동결 우회 봉인) |
| 폐지 청산 경로 | (미명시) | **상한 cap만**(floor 무·recovery=0 의 −100% 보존) | 휴지조각 총손실 −1.0 이 floor 로 마스킹되면 정직성 위반 |
| L2 SQL/python round | round(,2)=banker's | **ROUND_HALF_UP 명시**(DuckDB round=away-from-zero 와 동형) | 반센트 경계($1M 핀)서 SQL 술어와 python 분기 방지(리뷰 지적) |
| cap<=0 검증 | (없음) | `__post_init__` ValueError | 잘못된 cap 이 전 수익 음수 뭉갬 = 조용한 오설정 금지 |

### 구현 내용 (코드·정제는 A1p2-6에서 실데이터 실행)

- **storage.py**: `is_sentinel_bar(close, adj_factor)` L1(raw close ±band)·L2(adjusted $1M pin·HALF_UP)·L3(adj_factor≥100,000) per-bar 판별 + `SENTINEL_WHERE_SQL`(verify·clean 단일 SQL 출처) + `VerificationReport.sentinel_count` G-7 게이트. 상수 `Final`.
- **eodhd.py** `_row_to_bar`: `is_sentinel_bar` → `return None`(수집 시 per-bar drop).
- **clean_ohlc.py**: `_DEFECT_SQL` sentinel 술어 + `_clean_file` per-row drop(기존 트리 정제).
- **config.py**: `period_return_cap: Decimal`(env-tunable default 19.0·Final·`__post_init__` cap>0) → fingerprint.
- **engine.py** `_holding_period_return`: `return_cap` 파라미터 + `total += w * min(ret, return_cap)`(상한만). **benchmark.py** 동일 caller 갱신(engine·벤치 대칭 — G-3 비교 비대칭 방지).
- **s6_gate.py** `compute_rule_signature`: `period_return_cap` 8번째 필드(기본=정규 동결값) + run_s6_gate 가 `config.period_return_cap` 명시 전달.

### 검증 (After)

```
ruff check src tests        → All checks passed!
ruff format --check         → 90 files already formatted
mypy (strict)               → Success: no issues found in 90 source files
pytest                      → 375 passed
```

- diff stat(src+tests): config/engine/benchmark/storage/eodhd/clean_ohlc + 4 테스트파일.
- 리뷰 2종: convention-reviewer(BLOCKING 1=cap signature 포함→반영·HIGH Final→반영) + oh-my-claudecode:code-reviewer(MED floor 낙관편향→floor 제거로 근본 해소·MED L2 round→HALF_UP·MED cap 검증→`__post_init__`). 잔여 LOW(Tiingo defense-in-depth·cache 재빌드 순서·PG 동기)는 A1p2-6 롤아웃·후속.
- **완료 커밋**: (이 커밋 SHA — INDEX 동기)
- 회고: spec 의 floor 는 "tiny 분모 폭발 1차 방어선"으로 동결됐으나, ret≥−1 수학 경계를 간과한 과설계였다. 리뷰가 이를 잡아 cap-only 로 단순화 → 노브 1개·낙관편향 0. **데이터 보기 전 동결**은 지키되, 동결값 자체가 틀렸으면 리뷰로 교정(과적합과 구분).
