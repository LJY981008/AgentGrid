# 2026-06-16 — 코드리뷰 반영 (다차원 리뷰 wijex9w7a)

> 유형: 일반 구현 (리뷰 발견사항 반영). 라이브 0(모킹·픽스처). 미국주식 stockpick M1 data 층.

## 의도 / 배경

다차원 코드리뷰(wijex9w7a) 결과 중 **MUST/SHOULD** 항목을 반영한다. 핵심은
**양수성 게이트(HIGH — 금융 BLOCKING 성격)**: 음수/0 가격·adj_factor 가 현재 모든 게이트를
통과한다(실측 결함). 미국 EOD 가격엔 정당한 음수/0 가 없으므로 보수적으로 차단한다.

현재 상태(Before): 컨테이너 77 passed, 라이브 파일럿 통과.

### 반영 범위
- **A 양수성 게이트**: `_adjust.compute_adj_factor` adjusted<=0 분기 추가 / `storage` OHLC 음수·0 가격 검증 게이트 신설.
- **B**: `tests/test_adjust.py` 공유 헬퍼 단위테스트 신설.
- **C**: `write_daily_bars` docstring read-merge-write 증분 BLOCKING 경고(근본 전환은 M2 이월).
- **D**: `data.configure_logging()` 헬퍼로 httpx 토큰 URL 누출 가드 코드화 + 회귀 테스트.
- **E**: `test_pilot.py` mypy strict `type:ignore` 2건 정식 어노테이션으로 제거.
- **SHOULD**: eodhd `_to_symbol('')` 빈 ticker / `_map_exchange` dead code / `iter_universe` dedup 고지 / `_SQL_DUPLICATES` 주석.

### DEFER (이번 범위 밖 — 코드 변경 없이 기록만)
- write **read-merge-write 근본 전환** → M2 일일증분 티켓. (현 ticker별 파일 멱등은 "같은 ticker·연도 전체를 한 write 에" 라는 호출 계약 위에서만 안전 — docstring 경고로 핀.)
- iter_universe **부분실패 전파** 테스트 / 교차거래소 중복 테스트 → 여력 시 선택.

## Before / After

### 검증 실측 출력 (컨테이너 정본)
```
$ docker compose exec -T app sh -c 'ruff check src tests && ruff format --check src tests && mypy && pytest -q'
All checks passed!
18 files already formatted
Success: no issues found in 18 source files
89 passed in 0.56s
```
- Before: 77 passed → After: **89 passed** (+12: test_adjust 7, storage 음수가격 3, test_data_init 1, eodhd _to_symbol 1).
- ruff/format/mypy(strict) 무경고.

### diff stat
```
 src/stockpick/data/__init__.py | 30 +++  (configure_logging 헬퍼)
 src/stockpick/data/_adjust.py  | 19 +++  (adjusted<=0 가드)
 src/stockpick/data/eodhd.py    | 22 +++  (_to_symbol 빈ticker·_map_exchange dead·iter_universe 겹침)
 src/stockpick/data/pilot.py    |  4 +    (configure_logging 호출)
 src/stockpick/data/storage.py  | 34 +++  (음수/0 가격 게이트·docstring 경고·_SQL_DUPLICATES 주석)
 tests/test_eodhd.py            |  8 +    (_to_symbol 핀)
 tests/test_pilot.py            | 19 +++  (type:ignore 2건 제거 → 정식 어노테이션)
 tests/test_storage.py          | 36 +++  (음수/0 가격 게이트 3건)
 tests/test_adjust.py           | 신규   (공유 헬퍼 7건)
 tests/test_data_init.py        | 신규   (configure_logging 회귀 1건)
```

### A 양수성 게이트 동작 실증 (이번 핵심)
- `_adjust`: `compute_adj_factor(Decimal('-50'), Decimal('100'))` → 이전엔 `-0.5`(음수 계수). 이제 raw>0 통과 후 **adjusted<=0 분기**가 잡아 `1`(무수정) + WARNING. (test_adjust `test_adjusted_nonpositive_returns_one_with_warning`)
- `storage`: OHLC 전부 음수(open=-100,high=-90,low=-110,close=-105)는 **상대 관계(high>=low 등) OHLC 게이트를 통과**하지만, 신설 `_SQL_NONPOSITIVE_PRICE` 가 `가격<=0=1` 로 **VerificationError**. (test_storage `test_verify_fails_negative_price_passing_ohlc_relation`) — 0 가격도 동일 차단.

## DEFER (이번 미해결 — 후속 티켓)
- **write read-merge-write 근본 전환** → M2 일일증분. 현재는 `write_daily_bars` docstring 의 호출 계약 경고(같은 ticker·연도 전체를 한 write 에)로 핀했고, 코드는 안 바꿨다. 같은 ticker·연도를 별도 호출로 나누면 이전 행 소실 — 검증 게이트의 expected 대조로도 다른 배치 과거 행은 못 잡는다(한계 명시).
- iter_universe **부분실패 전파** 테스트 / 교차거래소 중복 테스트 → 여력 시 선택(이번 미수행).

## 회고
- A(양수성)는 금융 BLOCKING 성격: 게이트가 *상대* 관계만 봐 절대 음수/0 이 전부 통과하던 구멍을 _adjust(1차)·storage(2차) 이중으로 막았다. 미국 EOD 에 정당한 음수/0 가격 없음 → 보수적 차단이 옳다.
- D(httpx 토큰 가드)는 진입점 누락 위험을 헬퍼로 코드화 — 라이브러리 로거를 우리 코드가 못 끄는 한계를 진입점 1회 호출로 봉인.
- C·DEFER 의 read-merge-write 는 의도적으로 코드를 안 건드리고 계약 경고만 — 범위 확대(돈 걸린 저장 로직) 회피, M2 로 명확 이월.
