# 2026-06-17 하네스·docs 전수 감사 + 최신화

- **유형**: 하네스/인프라 (문서 동기화 — 코드 변경 src/__init__.py docstring 1건 포함)
- **관련 기획/이슈**: 사용자 요청 "하네스와 docs 확인하고 프로젝트 전체 점검·최신화"
- **시작 시점 커밋**: `b7c5b21` → **완료 커밋**: 이 커밋

## 의도 (Why)

M2 룰엔진·M3 API/webapp 가 커밋(ec9d3b0·2c9ab10·b7c5b21)됐으나 하네스·기획 문서가 그 진행을
못 따라가 **stale drift** 가 쌓였다. 9개 차원(CLAUDE.md·rules·agents·hooks·MOC·plans·work-history·
버전·위생)을 워크플로우로 병렬 감사(26 에이전트)하고 고위험 발견을 적대적 재검증한 뒤, 실측 확인된
drift 만 교정한다. 추측 금지 — 모든 수정은 실제 코드/파일 인용 기반.

## Before (실측 drift)

- **도메인 잔재(한국→미국)**: 2026-06-16 ADR-002 로 미장 전환됐으나 옛 한국 소스(FDR/pykrx/KRX/KIS)·
  "한국 주식" 표현이 7개 에이전트 description·python-conventions·logging-rules·api-spec-reference·
  README·AGENTS·.env.example·src/__init__.py docstring 에 잔존.
- **마일스톤 stale**: PLAN_STATUS 진행현황=B-contract·다음=B-pipeline(이미 완료) / Q1=Sharadar SEP
  (ADR-003 에서 EODHD 로 개정됨) / 작업재개-RESUME 가 M3 미반영·날짜 2026-06-16.
- **MOC 누락**: HOME.md 에 dev-log/2026-06-16·작업재개-RESUME·learning/ 섹션 미링크, research 요약이
  Sharadar SEP "현행" stale.
- **api-spec**: Tiingo 를 "현행 가격 소스"로 표기, 실제 주력 EODHD(ingest.py 가 EodhdSource 사용) 산문 부재.
- **죽은 drift 매핑**: harness-drift-check.sh:43 `frontend/src/`(레포에 없음, 실제 webapp/src).
- **backtest 오해**: CLAUDE.md 가 backtest 를 구현 모듈로 기술하나 실제 `__init__.py`만 있는 빈 패키지.
- **work-history SHA 미백필**: 7개 엔트리 완료 커밋 칸이 `<커밋 시 기입>` 플레이스홀더, M2 엔트리는 SHA 필드 누락.
- **Dockerfile 주석**: "런타임 의존성 없음(dependencies=[])"·"서버 없음" — M1~M3 진행 전 잔재.

## After (수정·검증)

- 도메인 잔재 전부 미국(Tiingo→EODHD·SEC EDGAR)으로 교정. CLAUDE.md M3/M4 라벨 통일(webapp=M3 완료)·
  backtest 빈패키지 명시·meta.validated=false 근본사유(backtest 미구현) 연결.
- PLAN_STATUS 진행현황·다음단계(M2 백테스트)·Q1(EODHD)·수정주가·한국 데이터소스 섹션·마일스톤 번호 정합 메모 갱신.
  M1·작업재개-RESUME(M3 위치·현 코드 상태 모듈·파일 인덱스·날짜) 갱신.
- HOME.md MOC 4건 추가/정정. api-spec-reference EODHD 전용 섹션 신설(rate limit 은 api-limits.json 위임,
  무료티어 숫자 단정 금지 — 환각 방지). 죽은 frontend/src 매핑 삭제.
- work-history 7엔트리 SHA 백필 + M2 엔트리 SHA 필드 추가. Dockerfile deps/CMD 주석 현행화.
- ⚠️ `docs/learning/`(사용자 소유)은 미변경 — HOME.md 에서 링크만 추가.
- **검증**: `ruff check && ruff format --check && mypy && pytest -q` → **All checks passed · 38 files · 134 passed**.
  `.claude/hooks/tests/run.sh` → **PASS=38 FAIL=0**(죽은 매핑 삭제 후 회귀 무영향).
- **변경 규모**: 28 파일, +123/-87 (문서 27 + src docstring 1).

## 회고

- **38케이스 사실 확인**: 감사 1차/검증 에이전트가 grep(`^t "`)으로 28 케이스라 "confirmed"했으나, run.sh
  실행 결과 PASS=38(inline `; t` 10건 누락 집계). → **grep 검증 < 실제 실행**. 38 표기 유지(수정 안 함).
- **EODHD 무료 20콜/일**: 감사 fix 가 권했으나 api-limits.json 은 해당 수치 비권위(별도 실측)라 기재 보류 —
  환각 방지 규칙 자체를 문서 수정에도 적용.
- 잔여 권고(미적용·후속 판단): ①api 계약↔프론트 types drift 매핑 추가 여부 ②마일스톤 번호 체계 §8 개정 vs
  커밋번호 유지(product-planner 위임) ③.env 로컬 파일의 미사용 KRX_API_KEY 줄 정리(사용자).
- 에이전트 .md 변경은 **세션 재시작 후 반영**(스킬은 /reload-skills).
