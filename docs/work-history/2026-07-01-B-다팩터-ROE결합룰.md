# 2026-07-01 Sub-project B — momentum × ROE 하드필터 결합 룰

- **유형**: 플랜모드 승인(브레인스토밍 → 6 전문가 다관점 비평 워크플로 REVISE-GO → 2 Explore 배선확인 → 플랜)
- **관련 기획/스펙**: 승인 스펙 [[../superpowers/specs/2026-07-01-B-다팩터-ROE결합룰-design]] / [[plans/stock-1st_plan]] §4.1 / 후속 of Sub-project A(재무 PIT 파운데이션) 완결
- **시작 커밋**: 32e9552(스펙) → 완료 커밋: (단계별)
- **승인 플랜 백업**: `/home/code/.claude/plans/graceful-spinning-shannon.md`(아래 요약)

## 의도/목적 — 왜

공식 게이트: momentum 단일팩터 = 깨끗한 26년 측정에서 진짜 무알파(validated=false·R-2 PASS). Sub-project A 완료로 임의 시점·종목 ROE 를 PIT(filed≤t)·생존편향-안전 획득(2.68M fact·12,607 cik·verify passed·정지점3 ROE율 2013+ ~24%). plan §4.1 "깨끗한 fail 후 다팩터 피벗" 도달. **목표**: momentum + **ROE>0 흑자 하드필터**(방식 C) 결합 룰이 momentum 단일팩터보다 나은지 validated 게이트로 정직 검증. 순서=ROE→momentum. G-5c=관측지표. 벤치=ROE-filtered(H3 대칭+H2 귀인 겸용).

## 계획 (승인 플랜 요약 — 전문=플랜파일)

- **P1 재무 팩터 하드닝**(H5·H8·H6): `latest_as_of` max_age_days(stale 상한)·`financial_factors` 동일 FY 강제·equity≤0 배제 명문화·성능 인덱싱(facts_by_cik+bisect·실측 벤치).
- **P2 ROE 필터+배선**(H7·H3): config `apply_roe_filter/min_roe/roe_max_age_days`·engine `_filter_by_roe`(liquidity 직후·momentum 전)·benchmark 대칭 필터(공유)·off=bit-identical.
- **P3 signature+게이트**(H1·H9·H10): compute_rule_signature factor_filter(on만·off=bit-identical·3자 동시갱신)·run_s6_gate financial_facts·S6GateResult G-5c **관측필드**(passed AND 아님)·main archive(다중검정).
- **P4 dry-run 3종**(H4): 커버·다중매칭·MNAR 계측(임계 조정 금지).
- **P5 pre-registration+게이트 1 run**: 동결 config(ROE-only·max_age 18개월) → validated 판정.

전문가 BLOCKING(6 관점·REVISE-GO): H1 signature 누수·H2 귀인·H3 벤치비대칭·H5 stale·H6 성능·H7 재배선·H8 FY불일치·H9 임계강등·H10 다중검정.

## Before — 수행 전 실측 (Explore 확인·2026-07-01)

- `rules/_financials.latest_as_of`(L52-78): `max_age_days` 없음 — `disclosed_at≤as_of` + `annual_only` 만·`max(period_end, disclosed_at)` 선택. **recency 상한 없어 stale 흑자 누설 가능**.
- `rules/factors.financial_factors`(L262-329): equity/income/shares 를 **각각 독립 `latest_as_of`**(L282-296) → 서로 다른 FY 조합 가능(equity FY24·income FY23). `equity>0` 가드(L296)로 자본잠식 **조용히 배제**(설계 미명시). 매 호출 전체 facts(2.68M) 완전 스캔.
- `backtest/engine._rank_at`(L184-224): constituents→liquidity(L185)→momentum score→rank_by_momentum→**cik 후부착(L224)**. ROE 필터 낄 자리 없음(cik 가 decile 후).
- `backtest/benchmark.equal_weight_universe`(L82-91): constituents→liquidity(L83)→tickers_with_data→equal_weight. **ROE 필터 없음**(엔진과 비대칭).
- `s6_gate.compute_rule_signature`(L475-516): payload 13필드(유동성 3·portfolio_pct·decile_min·period_return_cap 등). **factor_filter 없음**. `S6GateResult`(L164) G-1~R-2·G-5c 없음. `main` L722 `unlink`(시도이력 소멸).
- 현 전체 pytest 466+ passed·financial verify 2.68M fact 중복0 passed·validated=FALSE(momentum 단일).

## After — 수행 후 실측 (단계별 기입)

### P1 — 완료 (커밋 dde4f6d)

- `_financials.latest_as_of`: `max_age_days` 파라미터 — `(as_of-disclosed_at).days ≤ max_age_days` 만(초과=stale 배제·H5). None=하위호환.
- `factors.financial_factors`: `max_age_days` 전파 + ROE 산출 **동일 FY 강제**(equity·income period_end 일치·H8) + equity>0 자본잠식 배제 명문화 + `facts_by_cik` 1회 인덱싱(H6 성능·O(ciks×전체) 회피).
- 검증: ruff/mypy(strict) OK · 신규 TDD 5(FY불일치·stale·자본잠식·룩어헤드 회귀) green. diff: _financials +6·factors +34·tests +48.

### P2 — 완료 (엔진/벤치 배선·off=bit-identical)

- `config.BacktestConfig`: `apply_roe_filter: bool=False`·`min_roe: Decimal=0`·`roe_max_age_days: int|None=None` + `__post_init__` 검증(on·max_age≤0 차단). **동결 우회 방지 필드**.
- `engine.filter_by_roe`(신규·public 공유): 후보 ticker→cik(다중매칭 ValueError=배제+카운트·미해소=배제)→`financial_factors` ROE→`roe>min_roe` 만. `_rank_at` 이 **liquidity 직후·momentum 전** 호출(ROE→momentum 순서·H7). `run`/`_rank_at` 에 `financial_facts` 파라미터. **off(기본)=필터 미호출=momentum canonical bit-identical**.
- `benchmark.equal_weight_universe`: `identity`·`financial_facts` 파라미터 + liquidity 직후 **동일 `filter_by_roe` 공유**(엔진↔벤치 대칭·H3). apply_roe_filter 시 벤치=흑자∩유동 등가중(순수 ROE 베이스라인).
- 검증(컨테이너·진짜 게이트): ruff/mypy(strict) OK · pytest EXIT=0 · 신규 `test_backtest_roe_filter.py` 14 green — **off=bit-identical 회귀(최우선)**·on ROE→momentum 선택변경·전후보적자→평탄·벤치 대칭 on<off·룩어헤드/stale/자본잠식/다중매칭/미해소 명시배제. diff: engine +59·config +13·benchmark +18·tests +312(신규). (훅 post-work-check host-pytest 는 deps 미설치 환경 collection ERROR — 프로젝트는 Docker 실행이라 무관.)
- ⚠️ 사고: 검증 중 `printf ... | git stash` 오타로 워킹트리 P2 변경이 stash 로 치워져 import 실패 → `git stash pop` 복원. (git stash 는 stdin 무시하나 변경은 stash 함.)

### P3 — 완료 (signature+게이트 통합·H1·H9·H10)

- **H1 signature**: `compute_rule_signature` 에 apply_roe_filter/min_roe/roe_max_age_days → **on 일 때만 `factor_filter` 키**(roe_min·roe_max_age_days) 삽입. off(기본)=키 미삽입=payload 바이트동일=**momentum canonical bit-identical**(기존 45 s6_gate 테스트 무변동으로 봉인). `run_s6_gate` 가 config 3필드 전달. `ranking_rule_signature`·`canonical_gate_config` 옵셔널 pass-through(기본 off=momentum 불변·B 승격 다리). route(api/routes/ranking)=momentum 유지(ROE/PB=display factor·B validation 오클레임 방지). Decimal 런타임 import 추가(기본값 `Decimal(0)` 평가·TYPE_CHECKING 중복 제거).
- **H9 G-5c 관측**: `S6GateResult` 에 `g5c_coverage_rates`·`g5c_survivor_counts`·`g5c_mnar_skew`(기본 빈값). **passed AND 밖**(`evaluate_criteria` 불변 — validated=G-1~G-8·R-2 뿐). `_measure_g5c`(OOS fold별 유동 유니버스 재무 커버율·filter_by_roe 흑자 생존수) → apply_roe_filter 게이트에서만 `replace` attach(momentum=빈값). write/print_verdict 에 관측값 직렬화·출력. MNAR skew 전체(top-decile forward-return)는 P4 dry-run 담당.
- **H10 archive**: `main` `stale.unlink()` 앞 `_archive_stale_result`(이전 판정 JSONL append 보존·파싱실패 원문 래핑) → 반복 게이트 다중검정 이력 보존(조용한 덮어쓰기 차단).
- **financial_facts 배선**: run_s6_gate→walk_forward_by_cost/sensitivity_analysis→walk_forward(is/oos run)→engine.run + G-3 벤치(identity·facts·H3 대칭) + _reproducible(재현성도 동일 룰). validation.py TYPE_CHECKING FinancialFact 추가.
- 검증(컨테이너): ruff/mypy(strict) OK · pytest EXIT=0 · 신규 `test_s6_gate_factor_filter.py` 9 green(off=bit-identical·on≠off·max_age/min 발산·3자 off 일치·G-5c passed 무관·archive JSONL/원문보존·run_s6_gate off빈값/on채움+signature≠) · 기존 45 s6_gate 무회귀. diff: s6_gate +151·validation +4·tests +282(신규).

### P4~P5 — 미착수
