# 2026-07-01 Sub-project B — momentum × ROE 하드필터 결합 룰

- **유형**: 플랜모드 승인(브레인스토밍 → 6 전문가 다관점 비평 워크플로 REVISE-GO → 2 Explore 배선확인 → 플랜)
- **관련 기획/스펙**: 승인 스펙 [[../superpowers/specs/2026-07-01-B-다팩터-ROE결합룰-design]] / [[plans/stock-1st_plan]] §4.1 / 후속 of Sub-project A(재무 PIT 파운데이션) 완결
- **시작 커밋**: 32e9552(스펙) → 완료 커밋: (단계별)
- **승인 플랜 백업**: `/home/code/.claude/plans/graceful-spinning-shannon.md`(아래 요약)

## 의도/목적 — 왜

공식 게이트: momentum 단일팩터 = 깨끗한 26년 측정에서 진짜 무알파(validated=false·R-2 PASS). Sub-project A 완료로 임의 시점·종목 ROE 를 PIT(filed≤t)·생존편향-안전 획득(2.68M fact·12,607 cik·verify passed·정지점3 ROE율 2013+ ~24%). plan §4.1 "깨끗한 fail 후 다팩터 피벗" 도달. **목표**: momentum + **ROE>0 흑자 하드필터**(방식 C) 결합 룰이 momentum 단일팩터보다 나은지 validated 게이트로 정직 검증. 순서=ROE→momentum. G-5c=관측지표. 벤치=ROE-filtered(H3 대칭+H2 귀인 겸용).

## 계획 (승인 플랜 요약 — 전문=플랜파일)

- **P1 재무 팩터 하드닝**(H5·H8·H6): `latest_as_of` max_age_days(stale 상한)·`financial_factors` 동일 FY 강제·equity≤0 배제 명문화·성능 인덱싱(facts_by_cik+bisect·실측 벤치).
- **P2 ROE 필터+배선**(H7·H3): config `apply_roe_filter/min_roe/roe_max_age_days`·engine `_filter_by_roe`(liquidity 직후·momentum 전)·benchmark 대칭 필터(공유)·off=bit-identical.
- **P3 signature+게이트**(H1·H9·H10): compute_rule_signature factor_filter(on만·off=bit-identical·3자 동시갱신)·run_s6_gate financial_facts·S6GateResult G-5c **관측필드**(passed AND 아님)·main archive(다중검정).
- **P4 dry-run 3종**(H4): 커버·다중매칭·MNAR 계측(임계 조정 금지).
- **P5 pre-registration+게이트 1 run**: 동결 config(ROE-only·max_age 18개월) → validated 판정.

전문가 BLOCKING(6 관점·REVISE-GO): H1 signature 누수·H2 귀인·H3 벤치비대칭·H5 stale·H6 성능·H7 재배선·H8 FY불일치·H9 임계강등·H10 다중검정.

## Before — 수행 전 실측 (Explore 확인·2026-07-01)

- `rules/_financials.latest_as_of`(L52-78): `max_age_days` 없음 — `disclosed_at≤as_of` + `annual_only` 만·`max(period_end, disclosed_at)` 선택. **recency 상한 없어 stale 흑자 누설 가능**.
- `rules/factors.financial_factors`(L262-329): equity/income/shares 를 **각각 독립 `latest_as_of`**(L282-296) → 서로 다른 FY 조합 가능(equity FY24·income FY23). `equity>0` 가드(L296)로 자본잠식 **조용히 배제**(설계 미명시). 매 호출 전체 facts(2.68M) 완전 스캔.
- `backtest/engine._rank_at`(L184-224): constituents→liquidity(L185)→momentum score→rank_by_momentum→**cik 후부착(L224)**. ROE 필터 낄 자리 없음(cik 가 decile 후).
- `backtest/benchmark.equal_weight_universe`(L82-91): constituents→liquidity(L83)→tickers_with_data→equal_weight. **ROE 필터 없음**(엔진과 비대칭).
- `s6_gate.compute_rule_signature`(L475-516): payload 13필드(유동성 3·portfolio_pct·decile_min·period_return_cap 등). **factor_filter 없음**. `S6GateResult`(L164) G-1~R-2·G-5c 없음. `main` L722 `unlink`(시도이력 소멸).
- 현 전체 pytest 466+ passed·financial verify 2.68M fact 중복0 passed·validated=FALSE(momentum 단일).

## After — 수행 후 실측 (단계별 기입)

### P1~P5 — 미착수
